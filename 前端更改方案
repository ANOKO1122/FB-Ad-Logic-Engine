📐 FB 智投系统 - 前端页面设计方案
一、页面结构图 (Sitemap)
FB 智投系统
├── 🔐 认证模块（已完成）
│   ├── /login           登录页
│   ├── /register        注册页
│   └── /pending         待审核页
│
├── 📊 主功能区（MainLayout 布局）
│   │
│   ├── /dashboard            数据监控 [已完成，需小幅优化]
│   │   └── 核心模块：账户选择、层级筛选、时间范围、数据表格
│   │
│   ├── /rules                规则管理 [已完成，需小幅补充]
│   │   └── 核心模块：规则列表、规则编辑弹窗、SOP模板
│   │
│   ├── /logs                 📌 执行日志 [新增页面]
│   │   └── 核心模块：筛选器、日志表格、时间线视图、详情弹窗
│   │
│   └── /system               📌 系统状态 [新增页面]
│       └── 核心模块：健康指标卡片、同步状态表格、告警配置
│
└── 🔧 管理后台（仅管理员可见）
    ├── /admin/account-mapping    账户设置 [已完成]
    └── /admin/review             用户审核 [已完成]
FB 智投系统├── 🔐 认证模块（已完成）│   ├── /login           登录页│   ├── /register        注册页│   └── /pending         待审核页│├── 📊 主功能区（MainLayout 布局）│   ││   ├── /dashboard            数据监控 [已完成，需小幅优化]│   │   └── 核心模块：账户选择、层级筛选、时间范围、数据表格│   ││   ├── /rules                规则管理 [已完成，需小幅补充]│   │   └── 核心模块：规则列表、规则编辑弹窗、SOP模板│   ││   ├── /logs                 📌 执行日志 [新增页面]│   │   └── 核心模块：筛选器、日志表格、时间线视图、详情弹窗│   ││   └── /system               📌 系统状态 [新增页面]│       └── 核心模块：健康指标卡片、同步状态表格、告警配置│└── 🔧 管理后台（仅管理员可见）    ├── /admin/account-mapping    账户设置 [已完成]    └── /admin/review             用户审核 [已完成]
二、关键页面布局描述
2.1 执行日志页 /logs 【新增】
页面目标：审计日志可视化，快速定位"谁在什么时间对哪个广告做了什么操作"。
布局结构：
┌─────────────────────────────────────────────────────────────────┐
│  页面标题：执行日志                         [筛选器展开/收起]  │
├─────────────────────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 筛选区（折叠式 Panel）                                    │  │
│  │  ┌────────┬────────┬─────────┬─────────┬────────────┐   │  │
│  │  │ 账户   │ 规则   │ 状态    │ 时间范围 │  🔍 搜索   │   │  │
│  │  │[Select]│[Select]│[Select] │[DatePkr]│            │   │  │
│  │  └────────┴────────┴─────────┴─────────┴────────────┘   │  │
│  └──────────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 日志列表（DataTable）                                     │  │
│  │  ┌─────────┬────────┬────────┬────────┬──────┬────────┐ │  │
│  │  │ 时间    │ 规则名 │ 广告名 │ 动作   │ 状态 │ 操作   │ │  │
│  │  ├─────────┼────────┼────────┼────────┼──────┼────────┤ │  │
│  │  │ 10:23.. │ 止损1  │ xxx... │ 暂停   │ ✅   │ [详情] │ │  │
│  │  │ 10:22.. │ 扩量2  │ yyy... │ 加预算 │ ❌   │ [详情] │ │  │
│  │  └─────────┴────────┴────────┴────────┴──────┴────────┘ │  │
│  │                           [分页器：< 1 2 3 ... 10 >]      │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────────┐│  页面标题：执行日志                         [筛选器展开/收起]  │├─────────────────────────────────────────────────────────────────┤│  ┌──────────────────────────────────────────────────────────┐  ││  │ 筛选区（折叠式 Panel）                                    │  ││  │  ┌────────┬────────┬─────────┬─────────┬────────────┐   │  ││  │  │ 账户   │ 规则   │ 状态    │ 时间范围 │  🔍 搜索   │   │  ││  │  │[Select]│[Select]│[Select] │[DatePkr]│            │   │  ││  │  └────────┴────────┴─────────┴─────────┴────────────┘   │  ││  └──────────────────────────────────────────────────────────┘  │├─────────────────────────────────────────────────────────────────┤│  ┌──────────────────────────────────────────────────────────┐  ││  │ 日志列表（DataTable）                                     │  ││  │  ┌─────────┬────────┬────────┬────────┬──────┬────────┐ │  ││  │  │ 时间    │ 规则名 │ 广告名 │ 动作   │ 状态 │ 操作   │ │  ││  │  ├─────────┼────────┼────────┼────────┼──────┼────────┤ │  ││  │  │ 10:23.. │ 止损1  │ xxx... │ 暂停   │ ✅   │ [详情] │ │  ││  │  │ 10:22.. │ 扩量2  │ yyy... │ 加预算 │ ❌   │ [详情] │ │  ││  │  └─────────┴────────┴────────┴────────┴──────┴────────┘ │  ││  │                           [分页器：< 1 2 3 ... 10 >]      │  ││  └──────────────────────────────────────────────────────────┘  │└─────────────────────────────────────────────────────────────────┘
详情弹窗内容：
触发瞬时指标快照（花费、ROAS、CPA 等）
触发条件匹配详情
API 请求/响应原文（折叠展示）
错误信息（如有）
2.2 系统状态页 /system 【新增】
页面目标：一眼看清系统运行状态，支持手动触发同步/规则执行。
布局结构：
┌─────────────────────────────────────────────────────────────────┐
│  页面标题：系统状态                              [刷新按钮]     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐           │
│  │ 🟢 正常   │ │ 上次同步 │ │ 规则扫描 │ │ 队列积压 │           │
│  │  系统状态 │ │ 5分钟前  │ │ 耗时 2.3s│ │   0 条   │           │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘           │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│  ┌───────────────────────────────────────────────────────────┐ │
│  │ 账户同步状态                                               │ │
│  │  ┌──────────┬───────────┬──────────┬────────┬───────────┐│ │
│  │  │ 账户名    │ 账户ID    │ 最后同步  │ 时区   │ 状态      ││ │
│  │  ├──────────┼───────────┼──────────┼────────┼───────────┤│ │
│  │  │ 账户A    │ act_123.. │ 10分钟前  │ UTC+8  │ 🟢 正常   ││ │
│  │  │ 账户B    │ act_456.. │ 2小时前   │ UTC-5  │ 🟡 滞后   ││ │
│  │  └──────────┴───────────┴──────────┴────────┴───────────┘│ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│  ┌───────────────────────────────────────────────────────────┐ │
│  │ 定时任务状态                                               │ │
│  │  ┌────────────────┬───────────┬───────────┬─────────────┐│ │
│  │  │ 任务名          │ 运行周期   │ 上次执行   │ 下次执行    ││ │
│  │  ├────────────────┼───────────┼───────────┼─────────────┤│ │
│  │  │ 热数据同步      │ 每10分钟  │ 10:20     │ 10:30       ││ │
│  │  │ 冷数据归档      │ 每10分钟  │ 06:00     │ 明日06:00   ││ │
│  │  │ 规则引擎扫描    │ 每5分钟   │ 10:25     │ 10:30       ││ │
│  │  └────────────────┴───────────┴───────────┴─────────────┘│ │
│  │                                                           │ │
│  │  [🚀 手动触发规则执行]    [🔄 手动触发数据同步]            │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│  ┌───────────────────────────────────────────────────────────┐ │
│  │ 告警配置（可选模块，未来扩展）                             │ │
│  │  钉钉 Webhook URL: [••••••••••••••]  [测试连接] [保存]    │ │
│  └───────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────────┐│  页面标题：系统状态                              [刷新按钮]     │├─────────────────────────────────────────────────────────────────┤│                                                                 ││  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐           ││  │ 🟢 正常   │ │ 上次同步 │ │ 规则扫描 │ │ 队列积压 │           ││  │  系统状态 │ │ 5分钟前  │ │ 耗时 2.3s│ │   0 条   │           ││  └──────────┘ └──────────┘ └──────────┘ └──────────┘           ││                                                                 │├─────────────────────────────────────────────────────────────────┤│  ┌───────────────────────────────────────────────────────────┐ ││  │ 账户同步状态                                               │ ││  │  ┌──────────┬───────────┬──────────┬────────┬───────────┐│ ││  │  │ 账户名    │ 账户ID    │ 最后同步  │ 时区   │ 状态      ││ ││  │  ├──────────┼───────────┼──────────┼────────┼───────────┤│ ││  │  │ 账户A    │ act_123.. │ 10分钟前  │ UTC+8  │ 🟢 正常   ││ ││  │  │ 账户B    │ act_456.. │ 2小时前   │ UTC-5  │ 🟡 滞后   ││ ││  │  └──────────┴───────────┴──────────┴────────┴───────────┘│ ││  └───────────────────────────────────────────────────────────┘ ││                                                                 │├─────────────────────────────────────────────────────────────────┤│  ┌───────────────────────────────────────────────────────────┐ ││  │ 定时任务状态                                               │ ││  │  ┌────────────────┬───────────┬───────────┬─────────────┐│ ││  │  │ 任务名          │ 运行周期   │ 上次执行   │ 下次执行    ││ ││  │  ├────────────────┼───────────┼───────────┼─────────────┤│ ││  │  │ 热数据同步      │ 每10分钟  │ 10:20     │ 10:30       ││ ││  │  │ 冷数据归档      │ 每10分钟  │ 06:00     │ 明日06:00   ││ ││  │  │ 规则引擎扫描    │ 每5分钟   │ 10:25     │ 10:30       ││ ││  │  └────────────────┴───────────┴───────────┴─────────────┘│ ││  │                                                           │ ││  │  [🚀 手动触发规则执行]    [🔄 手动触发数据同步]            │ ││  └───────────────────────────────────────────────────────────┘ ││                                                                 │├─────────────────────────────────────────────────────────────────┤│  ┌───────────────────────────────────────────────────────────┐ ││  │ 告警配置（可选模块，未来扩展）                             │ ││  │  钉钉 Webhook URL: [••••••••••••••]  [测试连接] [保存]    │ ││  └───────────────────────────────────────────────────────────┘ │└─────────────────────────────────────────────────────────────────┘
2.3 数据监控页 /dashboard 【现有，微调】
当前状态：功能完整，无需大改。
建议优化点：
顶部新增「系统状态指示灯」：在账户选择器右侧，显示一个小圆点 🟢/🟡/🔴 表示系统是否正常
表格新增「规则命中」列（可选）：显示该广告是否有规则正在生效
2.4 规则管理页 /rules 【现有，微调】
当前状态：核心功能已完成（CRUD、条件配置、SOP 模板、Dry Run）。
建议优化点：
卡片底部新增「最近触发」信息：显示该规则最近一次触发的时间和结果
规则编辑弹窗底部新增「查看执行历史」按钮，跳转到 /logs?rule_id=xxx
2.5 侧边栏导航更新
现有导航：
数据监控
规则管理
[管理员] 账户设置
[管理员] 用户审核
新增导航项：
侧边栏导航
├── 数据监控      /dashboard
├── 规则管理      /rules
├── 📌 执行日志   /logs        [新增]
├── 📌 系统状态   /system      [新增]
├── ─── 分隔线 ───
├── [管理员] 账户设置   /admin/account-mapping
└── [管理员] 用户审核   /admin/review
侧边栏导航├── 数据监控      /dashboard├── 规则管理      /rules├── 📌 执行日志   /logs        [新增]├── 📌 系统状态   /system      [新增]├── ─── 分隔线 ───├── [管理员] 账户设置   /admin/account-mapping└── [管理员] 用户审核   /admin/review
三、组件映射表
功能点	所属页面	UI 组件形式	对应后端 API
账户选择	Dashboard, RuleManager	<select> 下拉框	GET /api/me/accounts (facebookApi)
层级筛选	Dashboard	ButtonGroup 切换按钮	-
时间范围	Dashboard	<select> 下拉框	-
广告数据展示	Dashboard	DataTable 表格 (10列指标)	GET /api/fb/insights
规则列表	RuleManager	CardGrid 卡片网格	GET /api/rules
规则启用/禁用	RuleManager	Switch 开关	PATCH /api/rules/:id/toggle
规则编辑	RuleManager	Modal 弹窗 + 表单	POST/PUT /api/rules
条件配置	RuleManager	动态表单行 (metric/operator/value/time_window)	-
SOP 模板	RuleManager	Button (止损/扩量模板)	-
Dry Run 开关	RuleManager	Switch 开关	isSimulation 字段
目标对象选择	RuleManager	级联下拉 + 多选列表	GET /api/fb/structure
📌 执行日志列表	Logs (新)	DataTable 表格 + 分页	GET /api/automation-logs
📌 日志筛选器	Logs (新)	折叠式筛选 Panel	-
📌 日志详情	Logs (新)	Modal 弹窗 (JSON 展示)	GET /api/automation-logs/:id
📌 健康指标卡片	System (新)	StatCard 卡片组	GET /api/system/health
📌 账户同步状态	System (新)	DataTable 表格	GET /api/system/health
📌 定时任务状态	System (新)	DataTable 表格	GET /api/admin/cron/status
📌 手动触发按钮	System (新)	Button (确认弹窗)	POST /api/admin/cron/execute
账户映射管理	AdminAccountMapping	DataTable + 批量导入	GET/POST /api/admin/account-mappings
用户审核	AdminReview	DataTable + 操作按钮	GET/POST /api/admin/users
四、交互逻辑简述
4.1 执行日志页 - 核心交互流程
用户进入 /logs 页面
    ↓
1. 页面加载：自动请求最近 50 条日志（默认今天）
    ↓
2. 筛选操作：
   - 选择「账户」→ 触发 API 重新请求，传递 account_id 参数
   - 选择「规则」→ 触发 API 重新请求，传递 rule_id 参数
   - 选择「状态」(成功/失败/跳过) → 触发 API 重新请求
   - 选择「时间范围」→ 触发 API 重新请求
    ↓
3. 查看详情：
   - 点击某行「详情」按钮
   - 弹出 Modal，展示：
     - 指标快照 (spend, roas, cpa...)
     - 触发条件 (规则条件 JSON)
     - API 请求/响应 (折叠展开)
     - 错误信息 (如有)
    ↓
4. 分页：点击页码，请求对应页数据
用户进入 /logs 页面    ↓1. 页面加载：自动请求最近 50 条日志（默认今天）    ↓2. 筛选操作：   - 选择「账户」→ 触发 API 重新请求，传递 account_id 参数   - 选择「规则」→ 触发 API 重新请求，传递 rule_id 参数   - 选择「状态」(成功/失败/跳过) → 触发 API 重新请求   - 选择「时间范围」→ 触发 API 重新请求    ↓3. 查看详情：   - 点击某行「详情」按钮   - 弹出 Modal，展示：     - 指标快照 (spend, roas, cpa...)     - 触发条件 (规则条件 JSON)     - API 请求/响应 (折叠展开)     - 错误信息 (如有)    ↓4. 分页：点击页码，请求对应页数据
4.2 系统状态页 - 核心交互流程
用户进入 /system 页面
    ↓
1. 页面加载：
   - 请求 GET /api/system/health 获取健康数据
   - 请求 GET /api/admin/cron/status 获取定时任务状态
    ↓
2. 健康指标卡片区：
   - 系统状态：🟢 正常 / 🔴 Token锁定
   - 最后同步：显示距今多久
   - 规则扫描耗时：显示毫秒数
   - 队列积压：显示待执行任务数
    ↓
3. 账户同步状态表：
   - 每行显示：账户名、账户ID、最后同步时间、时区、状态
   - 状态判断：
     - 🟢 正常：最后同步 < 15 分钟
     - 🟡 滞后：15 分钟 ~ 1 小时
     - 🔴 异常：> 1 小时未同步
    ↓
4. 手动触发：
   - 点击「手动触发规则执行」
   - 弹出确认对话框："确定立即执行所有启用的规则？"
   - 确认后调用 POST /api/admin/cron/execute
   - 显示 Toast："已触发执行，请在日志页查看结果"
用户进入 /system 页面    ↓1. 页面加载：   - 请求 GET /api/system/health 获取健康数据   - 请求 GET /api/admin/cron/status 获取定时任务状态    ↓2. 健康指标卡片区：   - 系统状态：🟢 正常 / 🔴 Token锁定   - 最后同步：显示距今多久   - 规则扫描耗时：显示毫秒数   - 队列积压：显示待执行任务数    ↓3. 账户同步状态表：   - 每行显示：账户名、账户ID、最后同步时间、时区、状态   - 状态判断：     - 🟢 正常：最后同步 < 15 分钟     - 🟡 滞后：15 分钟 ~ 1 小时     - 🔴 异常：> 1 小时未同步    ↓4. 手动触发：   - 点击「手动触发规则执行」   - 弹出确认对话框："确定立即执行所有启用的规则？"   - 确认后调用 POST /api/admin/cron/execute   - 显示 Toast："已触发执行，请在日志页查看结果"
4.3 规则管理页 - 核心交互流程（已有，补充说明）
创建规则流程：
    ↓
1. 点击「新建规则」→ 打开编辑弹窗
    ↓
2. 填写基本信息：
   - 规则名称（必填）
   - Dry Run 开关（默认关闭）
    ↓
3. 选择作用对象：
   - 选择账户 → 自动加载该账户的广告列表
   - 选择层级 (广告/广告组/广告系列) → 刷新列表
   - 多选目标对象（支持搜索、全选、分页加载）
    ↓
4. 配置触发条件：
   - 点击「止损模板」或「扩量模板」→ 自动填充条件
   - 或手动添加条件行 (metric/time_window/operator/value)
   - 选择逻辑关系 (AND/OR)
    ↓
5. 配置执行动作：
   - 添加动作行 (暂停/启用/增加预算/减少预算)
   - 预算类动作需填写百分比
    ↓
6. 点击「保存规则」→ 调用 API → 关闭弹窗 → 刷新列表
创建规则流程：    ↓1. 点击「新建规则」→ 打开编辑弹窗    ↓2. 填写基本信息：   - 规则名称（必填）   - Dry Run 开关（默认关闭）    ↓3. 选择作用对象：   - 选择账户 → 自动加载该账户的广告列表   - 选择层级 (广告/广告组/广告系列) → 刷新列表   - 多选目标对象（支持搜索、全选、分页加载）    ↓4. 配置触发条件：   - 点击「止损模板」或「扩量模板」→ 自动填充条件   - 或手动添加条件行 (metric/time_window/operator/value)   - 选择逻辑关系 (AND/OR)    ↓5. 配置执行动作：   - 添加动作行 (暂停/启用/增加预算/减少预算)   - 预算类动作需填写百分比    ↓6. 点击「保存规则」→ 调用 API → 关闭弹窗 → 刷新列表
五、后端 API 需求对照表
以下是前端需要调用的 API 及其当前状态：
API 端点	用途	状态
GET /api/rules	获取规则列表	✅ 已实现
POST /api/rules	创建规则	✅ 已实现
PUT /api/rules/:id	更新规则	✅ 已实现
DELETE /api/rules/:id	删除规则	✅ 已实现
PATCH /api/rules/:id/toggle	启用/禁用规则	✅ 已实现
GET /api/admin/cron/status	获取定时任务状态	✅ 已实现
POST /api/admin/cron/execute	手动触发规则执行	✅ 已实现
GET /api/automation-logs	获取审计日志列表	⚠️ 待实现
GET /api/automation-logs/:id	获取日志详情	⚠️ 待实现
GET /api/system/health	获取系统健康状态	⚠️ 待实现
六、视觉规范遵循说明
6.1 色彩系统（沿用现有）
主色调：中性色（黑/白/灰）
语义色：
成功/启用：var(--secondary-color) 绿色
警告：var(--warning-color) 黄色
危险/删除：var(--danger-color) 红色
主操作：var(--primary-color) 蓝色
6.2 组件复用
所有新页面使用现有 .card, .data-table, .btn, .select 等类名
弹窗复用 .modal-overlay, .modal-content 结构
筛选器复用 Dashboard 的 .filter-item, .btn-group 样式
6.3 间距与对齐
页面内边距：24px（沿用 MainLayout 的 .page-container）
卡片间距：20px (gap)
表格行高：48px
严格 8px 基准网格对齐
七、实施优先级建议
优先级	页面/功能	理由
P0	/logs 执行日志页	审计是核心需求，直接支撑"可追溯"目标
P1	/system 系统状态页	健康监控是运维必备，且后端 API 部分已有
P2	侧边栏导航更新	简单改动，串联新页面
P3	Dashboard 状态指示灯	锦上添花，非必须
P4	RuleManager 触发历史	锦上添花，非必须
八、总结
本方案覆盖了您 DEV_PLAN.md 和 TASKS.md 中提到的所有前端需求：
✅ 数据监控 - 已完成，无需大改
✅ 规则管理 - 已完成，包含级联选择、条件配置、SOP 模板、Dry Run
📌 执行日志 - 新增页面，完成审计日志可视化
📌 系统状态 - 新增页面，完成健康看板
✅ 账户设置/用户审核 - 已完成