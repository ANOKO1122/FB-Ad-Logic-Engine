FB Ad-Intelligence Server 架构方案（最终修订版）
第一部分：方案概括版 (Summary)
1. 数据架构层（核心升级）
冷热数据分离 (Hot/Cold Separation)：
热数据 (Today)：每 15分钟 轮询一次，保证当天数据的准实时性（平衡 FB 数据回传延迟与 API 额度）。
冷数据 (History)：次日早晨 06:00 执行归档任务，将昨天的数据固化到本地历史表，查询历史报表时零 API 消耗。
读写分离逻辑：监控大屏和规则引擎只读本地数据库；执行动作时才通过集中式动作队列调用 FB API。
智能熔断机制 (Circuit Breaker)：放弃复杂的微服务监控，采用计数器策略。若 Token 连续 3 次报权鉴错误（Code 190），系统自动死锁并报警，停止无效重试。
数据新鲜度校验：执行规则前检查 last_sync_time，若数据陈旧（>30分钟）自动跳过执行，防止误判。
2. 规则配置层（时空精准化）
时空一致性 (Time-Space Consistency)：引入 Luxon 库。所有规则判断（如“今日花费”）严格基于广告账户所在时区计算，而非服务器时间，杜绝跨时区误操作。
多层级精细指派：支持“账户 -> 系列 -> 组 -> 广告”层级筛选，配合状态过滤器。
复合逻辑与 SOP：支持 AND/OR 组合判断，内置行业成熟的止损/扩量模板。
3. 动作执行层（安全与闭环）
用户自定义冷却期：防止短时间内重复操作（如加预算）。
预算安全护栏：自动扩量时强制金额封顶。
Dry Run (模拟运行)：支持“只记录不执行”模式。
异常处理：针对除零、空值进行保护；人工干预后的自动豁免机制。
4. 反馈与监控层
详细审计日志：记录每一次自动操作的快照数据。
IM 机器人通知：钉钉/飞书实时报警，支持 Token 熔断紧急通知。
第二部分：详细拆解版 (Detailed Implementation)
1. 数据架构层
数据搬运工 (Data Ingestor) - 冷热分离策略
热数据同步 (Real-time)：
频率：设定 cron 为每 15 分钟一次。这是性价比最高的频率，因为 FB 的转化数据回传本身就有延迟，过快请求既浪费额度又拿不到最新转化。
目标：更新 realtime_snapshots 表（仅存今日数据）。
冷数据归档 (ETL)：
频率：每天早上 06:00 (账户当地时间) 触发。选择这个时间点是为了等待 FB 昨天的归因数据稳定（Settled）。
逻辑：将昨天的各项指标（Spend, CTR, ROAS 等）汇总写入 daily_stats 历史表。
优势：当运营查询“昨天”或“过去7天”报表时，直接读本地历史表，毫秒级响应且不消耗 API。
Token 智能熔断机制 (Simple Circuit Breaker)
逻辑：不依赖外部微服务，在 Node.js 主进程中维护一个全局计数器 authFailureCount。
执行流程：
每次请求 FB API 时捕获错误。
如果错误码为 190 (Invalid OAuth Access Token)：
authFailureCount++
如果 authFailureCount >= 3：触发系统死锁 (System Lock)，停止所有 Data Ingestor 和规则扫描，并通过 IM 机器人发送“红色警报”。
如果请求成功：重置 authFailureCount = 0。
意义：简单粗暴地防止 Token 失效后的无效重试，避免被 FB 判定为恶意攻击。
2. 规则配置层
时空一致性 (Timezone Handling via Luxon)
背景：服务器在国内，账户在美东/美西/欧洲，时间标准不一会导致“今日花费”计算错误。
做法：
存储：在 accounts 表存储 FB 返回的 timezone_name (如 America/Los_Angeles)。
计算：使用 Luxon 库。在规则引擎计算逻辑时，使用 DateTime.now().setZone(account_timezone) 获取该账户视角的“当前时间”和“今日起始点”。
意义：确保规则永远在正确的“当地时间”触发，消除时区差异带来的数据漂移。
人工干预豁免 (Manual Override)
逻辑：避免系统与运营“打架”（系统关了，运营开了，系统又关了）。
做法：
同步数据时，检测到广告状态从 PAUSED 变 ACTIVE 且无系统操作日志，标记为“人工操作”。
规则执行时，若发现该广告在 24 小时内有人工操作记录，自动跳过执行关停类规则。
3. 动作执行层 & 监控
集中频率管理
串行执行：37 个账户的数据同步使用 for...of 串行处理，避免并发峰值。
动作缓冲：所有修改操作写入队列，按 0.5s 间隔逐个释放请求。
归因数据对齐
参数配置：所有 Insight 请求强制携带 use_account_attribution_setting=true，确保抓取的数据与 FB 广告后台完全一致。
异常监控看板
监控点：
Data Ingestor 上次成功运行时间（监控搬运工是否偷懒）。
Token 连续失败次数（监控账号安全）。
系统当前是否处于 Locked 状态。