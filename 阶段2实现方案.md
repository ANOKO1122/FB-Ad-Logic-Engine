# 阶段2实现方案：规则执行摘要（系统层可观测性）

## 📋 实现步骤

### 步骤1：执行 SQL 迁移（⚠️ 你手动执行）

**文件**：`server/db/migrations/012_create_rule_execution_summaries.sql`

```bash
# 在 MySQL 中执行
mysql -u your_user -p your_database < server/db/migrations/012_create_rule_execution_summaries.sql
```

或者在 MySQL 客户端中直接执行 SQL 文件内容。

---

### 步骤2：修改 cronService.js（代码修改）

需要修改 3 个地方：

#### 2.1 导入摘要服务（文件顶部）

在 `cronService.js` 顶部添加：

```javascript
import { 
  insertRuleExecutionSummary, 
  sanitizeErrorMessage, 
  generateRunId 
} from './ruleExecutionSummaryService.js'
```

#### 2.2 修改 executeAllRules（生成并传递 run_id）

在 `executeAllRules` 函数开头（约第 310 行）添加：

```javascript
export async function executeAllRules(options = {}) {
  const { force = false, accountIds = null } = options
  
  // ✅ 生成 run_id（同一轮执行共享）
  const runId = generateRunId()
  console.log(`🆔 本次规则执行 run_id: ${runId}`)
  
  const startTime = Date.now()
  // ... 现有代码 ...
  
  // 在调用 executeRulesForAccount 时传递 runId
  const results = await Promise.allSettled(
    targetAccountIds.map(accountId => 
      executeRulesForAccount(accountId, { force, runId })  // ✅ 传递 runId
    )
  )
```

#### 2.3 修改 executeRulesForAccount（添加摘要记录）

在 `executeRulesForAccount` 函数中，需要：

1. **接收 runId 参数**（约第 135 行）：
```javascript
export async function executeRulesForAccount(accountId, options = {}) {
  const { force = false, runId = null } = options  // ✅ 接收 runId
  
  // ✅ 如果没有传入 runId（事件触发场景），生成新的
  const currentRunId = runId || generateRunId()
```

2. **在每个规则执行的分支记录摘要**（约第 243 行开始）：

需要覆盖以下分支：
- 用户不存在或未激活（第 212-214 行）
- 用户无权访问账户（第 234-236 行）
- 规则账户不匹配（第 247-249 行）
- 规则评估匹配0个广告（第 275-276 行）
- 规则评估异常（第 281-284 行）
- 执行动作异常（第 271-273 行）
- 用户处理异常（第 286-289 行）

具体修改见下面的代码片段。

---

### 步骤3：修改 ingestorService.js（事件触发场景）

在 `ingestorService.js` 约第 2761 行，事件触发时也需要生成 run_id：

```javascript
// 修改前
await executeRulesForAccount(accountId, { force: false })

// 修改后
const { generateRunId } = await import('./cronService.js')
const runId = generateRunId()
await executeRulesForAccount(accountId, { force: false, runId })
```

**注意**：这里需要从 `ruleExecutionSummaryService.js` 导入 `generateRunId`，或者从 `cronService.js` 导出它。

---

### 步骤4：创建查询接口（仅 admin 可见）

在 `server/routes/system.js` 中添加摘要查询接口。

---

## 🔧 详细代码修改

### executeRulesForAccount 完整修改示例

```javascript
// 在规则执行循环中（约第 243 行）
for (const rule of userRules) {
  const startTime = Date.now()
  const summary = {
    runId: currentRunId,
    ruleId: rule.id,
    ruleName: rule.ruleName,
    accountId: lockedAccountId,
    userId: parseInt(userId),
    ownerId: user.owner_id || 0,
    matchedCount: 0,
    executedCount: 0,
    failedCount: 0,
    skippedCount: 0,
    status: null,
    skipReason: null,
    skipDetails: null,
    errorMessage: null,
    durationMs: 0,
    evaluatedAt: new Date()
  }
  
  try {
    // 分支1：规则账户不匹配
    const ruleAccountId = rule.accountId
    if (ruleAccountId && ruleAccountId !== lockedAccountId) {
      summary.status = 'skipped'
      summary.skipReason = 'account_mismatch'
      summary.skipDetails = {
        rule_account_id: ruleAccountId,
        locked_account_id: lockedAccountId
      }
      summary.durationMs = Date.now() - startTime
      await insertRuleExecutionSummary(summary)
      console.log(`   ⏭️  [${lockedAccountId}] 规则 "${rule.ruleName}" (ID: ${rule.id}) 指定的账户 ${ruleAccountId} 不匹配，跳过`)
      continue
    }
    
    // 执行规则评估
    const matchedAds = await ruleEngine.evaluateRule(rule, lockedAccountId)
    
    if (Array.isArray(matchedAds) && matchedAds.length > 0) {
      summary.matchedCount = matchedAds.length
      summary.status = 'matched'
      
      // 执行动作
      try {
        const execStats = await executeActionsForRule({
          rule,
          matchedAds,
          accountId: lockedAccountId,
          ownerId: user.owner_id || 0
        })
        
        summary.executedCount = execStats.successCount || 0
        summary.failedCount = execStats.failCount || 0
        summary.durationMs = Date.now() - startTime
        await insertRuleExecutionSummary(summary)
        
        accountMatched += matchedAds.length
        accountExecuted++
      } catch (execError) {
        summary.status = 'error'
        summary.skipReason = 'error'
        summary.failedCount = matchedAds.length
        summary.errorMessage = sanitizeErrorMessage(execError.message)
        summary.durationMs = Date.now() - startTime
        await insertRuleExecutionSummary(summary)
        
        console.error(`    ❌ [${lockedAccountId}] 执行规则动作失败 "${rule.ruleName}":`, execError.message)
        accountErrors++
      }
    } else {
      // 分支：0 匹配
      summary.status = 'no_match'
      summary.skipReason = 'no_match'
      summary.durationMs = Date.now() - startTime
      await insertRuleExecutionSummary(summary)
      
      accountSkipped++
    }
    
    // 更新规则的上次执行时间
    await updateRuleLastExecutedAt(rule.id)
  } catch (error) {
    // 分支：规则评估异常
    summary.status = 'error'
    summary.skipReason = 'error'
    summary.errorMessage = sanitizeErrorMessage(error.message)
    summary.durationMs = Date.now() - startTime
    await insertRuleExecutionSummary(summary)
    
    console.error(`    ❌ [${lockedAccountId}] 执行规则 "${rule.ruleName}" 失败:`, error.message)
    accountErrors++
  }
}
```

**还需要在用户循环外层记录**：
- 用户不存在（第 212-214 行）
- 用户无权访问（第 234-236 行）

这两个分支需要在用户循环开始前记录，因为此时还没有具体的规则。

---

## ✅ 验收标准

1. **写入验证**：触发规则执行后，`rule_execution_summaries` 表至少新增 1 行
2. **覆盖验证**：至少覆盖 6 类原因（cooldown/no_permission/account_mismatch/user_not_found/no_match/error）
3. **run_id 验证**：同一轮执行的所有摘要共享同一个 run_id
4. **查询验证**：admin 可以通过接口查询摘要（下一步实现）

---

## 📝 下一步

完成代码修改后，可以：
1. 触发一次规则执行
2. 查询数据库验证摘要是否正确写入
3. 实现查询接口（仅 admin 可见）
