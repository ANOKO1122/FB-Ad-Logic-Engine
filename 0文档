下面按“我建议的优先顺序”给你展开：先做哪块、怎么做、改哪里、原理/原因/理由/优缺点/区别。你可以直接当成执行计划。

优先顺序（推荐）

1. 规则目标解析改造（最高优先级）
2. 前端状态筛选补齐（稳定性修复）
3. 结构索引（可选优化）
4. 全量回填/定期回填策略（运维化）
你们已经完成“写入时补齐 + 7天回填”的验证，这部分已从“方案”变为“已落地”。所以下一步应该转向“规则目标解析”与“前端链路稳定”。

## 1) 规则目标解析改造（第一优先级）
为什么先做

- 这是“0 匹配”的直接根因：规则目标被 ad_snapshots（spend 子集）绑死。
- 结构表已全量，改成用结构表解析目标，规则命中立刻稳定。
原理

- 目标层级 = 执行动作层级，不再把 campaign/adset 目标“展开成 ad_id 再执行”。
- 结构镜像表提供稳定关系；事实表只提供指标。
怎么做（改动思路）

- 找到规则目标解析函数（你们之前定位在 ruleEngineDispatcher.js ）。
- 当前逻辑：
  - campaign/adset → 从 ad_snapshots 反查 ad_id
- 改为：
  - campaign → 从 structure_ads 取 ad_id（或直接把 action 下发到 campaign）
  - adset → 从 structure_ads 取 ad_id（或直接 action 到 adset）
  - ad → 直接 ad_id
理由

- 结构表是全量关系，可靠；ad_snapshots 只覆盖 spend>0 子集。
- 用结构表会明显提升规则稳定性和可解释性。
优点

- 立即解决“规则命中=0”的问题
- 不新增 API 调用
- 逻辑更符合你们“结构镜像=真相源”的设计
缺点

- 需要改动规则目标解析逻辑（影响核心路径，需回归测试）
区别（与旧做法）

- 旧：目标解析依赖 spend 子集 → 常漏
- 新：目标解析依赖结构全量 → 稳定完整
## 2) 前端状态筛选补齐（第二优先级）
为什么做

- 你之前遇到的 “scopeIncludePaused not defined” 是前端错误，会影响结构筛选。
- include_paused 不传会让用户“看不到暂停对象”，影响规则目标配置。
原理

- 前端 UI 状态需要与后端 include_paused 参数一致。
怎么做

- RuleManager.vue：返回 scopeIncludePaused、补 onScopeStatusChange
- facebookApi.js：getStructureObjects 传 include_paused
- 这件事是“体验稳定性”修复，低风险但高必要性
优点

- 消除控制台报错
- 目标选择更可控（含暂停）
缺点

- 无业务逻辑收益，但能减少误操作
区别

- 之前：筛选条件丢失
- 之后：筛选与后端一致
## 3) 结构索引（可选优化）
为什么放第三

- 不是必要条件，但能让“结构查询入口统一”。
- 不改核心流程，只是统一结构数据的访问方式。
原理

- 用视图/索引统一结构对象（id/type/name/status），减少多处拼装。
优点

- 结构调用逻辑更统一、可维护
- 方便前端/规则复用
缺点

- 初期工程量不小
- 非“立刻见效”的功能
区别

- 之前：多处拼结构
- 之后：单入口结构索引
## 4) 全量/定期回填策略（运维化）
为什么最后做

- 你已完成“补齐 + 7 天回填”验证，这一步只是变成长期策略。
原理

- 定期把 ad_snapshots / daily_stats 缺失的 campaign_id/ad_set_id 补齐
- 仅修 NULL/空串，不全表更新
优点

- 保证历史数据质量
- 提升多天规则稳定性
缺点

- 需要控制更新范围和运行频率
# 你可以直接发给 AI 的“执行说明模板” 先改规则目标解析：把 campaign/adset→ad_id 的解析从 ad_snapshots 改为 structure_ads（结构全量），避免 spend 子集导致 0 匹配。
 然后修前端 include_paused 流程：RuleManager.vue 返回 scopeIncludePaused + onScopeStatusChange，facebookApi.js 传 include_paused。
 最后再考虑结构索引视图（非必需）。
 原理：结构镜像是关系真相源；事实表只做指标。
 优点：规则命中稳定、无需新增 API、语义一致。缺点：规则解析逻辑需回归测试。
如果你愿意，我可以按你说的顺序把第 1 项拆成“具体改动文件 + 伪代码 + 验收测试”的完整实现清单。