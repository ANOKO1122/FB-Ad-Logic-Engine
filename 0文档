FB 广告自动化规则系统 — 使用说明书（运营 / 老板版）
一、系统是干什么的？
一句话：根据你设好的条件，自动对 Facebook 广告做“暂停、开启、加钱、减钱”等操作，不用人一直盯着。
谁用：运营、投手、老板（看报表和规则效果）。
典型场景：
花了不少钱但 ROAS 很差 → 自动暂停；
表现好的广告 → 自动加预算；
只花钱没点击 → 自动暂停。
二、怎么用？（大流程）
登录 → 进「规则管理」页面。
新建规则 或 从模板创建（有现成的止损、扩量、空耗等模板）。
选账户：这条规则管哪些广告账户（可多选）。
选作用对象：管到「广告 / 广告组 / 广告系列」哪一层；可以按条件动态筛，也可以手动勾具体对象。
设触发条件：例如「今天花费 > 20 且 ROAS < 0.5」。
设执行动作：例如「暂停广告」「增加预算 20%」。
设执行频率、执行时间（可选）：多久对同一广告再动一次、每天几点到几点允许执行。
保存并启用，系统会按设定自动跑。
其他常用入口：
仪表盘：看整体情况。
执行日志：看每条规则什么时候、对哪条广告、执行了什么动作。
系统状态（管理员）：看定时任务、同步是否正常。
三、功能说明（大白话）
1. 动态筛选
是什么：不用每次手动勾选几百条广告，用「条件」让系统自动筛出要管的范围。
能筛什么（在「监控范围条件」里）：
状态：只要在投的、只要暂停的、或两者都要。
名称：名称里包含某几个词、不包含某几个词（例如排除 test、old）。
创建时间：只管「最近 N 小时 / 天」创建的。
效果：
新上的广告只要符合条件，会自动被这条规则管到，不用再改规则。
适合「按命名规范管理」：名字带 US_Men_、2026_ 的自动管，带 test 的自动不管。
注意：
动态筛选会有一个「匹配上限」（例如 1000 条），防止一次管太多。
规则列表里会显示「动态匹配数」和状态（正常 / 超限等），便于排查。
2. 「2 秒抖动」同步（防抖）
系统里有两处「等一小会儿再动」的设计，避免你改一下它就立刻狂请求、界面卡顿：
（1）监控范围条件 — 2 秒防抖
什么时候：你在规则里改了「监控范围条件」（状态、名称包含/不包含、创建时间等），或者换了已选账户、目标层级。
做什么：改完约 2 秒内不再改的话，系统会自动「应用条件」：按新条件重新拉列表并勾选匹配的广告/广告组/系列。
对你：不用每次都点「应用」；改完条件稍等 2 秒，列表会自动更新并勾好。
（2）规则保存后 — 动态筛选刷新防抖
什么时候：你保存了一条带「动态筛选」的规则，且这条规则关联多个账户。
做什么：系统不会立刻给所有账户都刷新，而是按账户排队、隔一小段时间再刷新（可配置，例如十几秒量级），避免保存一瞬间打爆接口。
对你：保存后看到「动态匹配数」或状态稍后才更新是正常的；多账户时尤其会分批更新。
3. 模板
是什么：把「触发条件 + 执行动作」打包成一套方案，创建规则时选一个模板，条件与动作会预填好，你再微调即可。
内置示例（具体以系统内为准）：
止损：花费 > 20 且 ROAS < 0.5（今天）→ 暂停广告。
扩量：ROAS > 2（近 3 天）→ 增加预算 20%。
空耗：花费 > 0.5 且点击 = 0（今天）→ 暂停广告。
谁能用：
所有人：创建/编辑规则时选用模板（只读）。
管理员：在「模板管理」里增删改模板（例如加一个「大促止损」模板）。
怎么用：新建规则时选「从模板创建」→ 选一个模板 → 自动带出条件和动作 → 再改作用对象、账户、执行频率等。
4. 排除对象
是什么：在「最终要管的范围」里，再划掉一部分，不让规则动它们。
公式：
最终作用范围 = 动态筛出来的 ∪ 你手动勾选的 − 排除名单
典型用法：
动态筛了 500 条广告，但其中有 10 条是老板指定不能动的 → 把这 10 条加到「排除名单」。
某几条广告要单独调，不想被这条规则管 → 也放进排除名单。
操作：在规则编辑里找到「排除名单（可选）」→ 在下方列表里勾选要排除的广告/广告组/系列 → 保存。支持「清空排除名单」一键取消所有排除。
5. 执行动作优先级（同一条广告被多条规则命中时）
问题：同一条广告可能同时满足多条规则（例如一条要暂停、一条要加预算），系统只能执行一个动作，谁先？
规则（从高到低）：
暂停广告 最优先（先保本、少花钱）。
开启广告 其次。
减少预算 再其次。
增加预算 再再其次。
设置预算（设为固定金额）最后。
同一优先级：
若两条规则动作类型相同（比如都是暂停），则按「规则 ID 更小的」那条执行。
输掉的那条规则会记成「被压制」，不会执行动作，但会算「触发过一次」（占冷却，见下）。
对你：
想「宁可先停掉再慢慢调」：把止损类规则放在前面或保证是「暂停」动作即可。
一条规则里可以配多个动作（如既有暂停又有减预算），但真正执行时只会执行其中「最狠」的那一个（按上面优先级选）。
6. 预算相关的几种效果（场景化说明）
系统支持三种预算动作，并且都有「上下限」保护，避免调过头。
（1）增加预算（按比例或按金额）
比例：例如 +20%，当前 100 刀 → 调成 120 刀。
金额：例如 +5 刀，当前 100 刀 → 105 刀。
上限（预算护栏）：可以设「最高不超过 X 刀/天」。
若当前已经 ≥ 上限：不再加，直接跳过，不调 API。
若加完超过上限：只加到上限为止。
（2）减少预算（按比例或按金额）
比例：例如 -20%，当前 100 刀 → 80 刀。
金额：例如 -5 刀。
下限（预算护栏）：可以设「最低不低于 X 刀/天」。
若当前已经 ≤ 下限：不再减，直接跳过。
若减完低于下限：只减到下限为止。
（3）设置预算（设为固定值）
含义：把预算直接设成某个数（例如 30 刀），不按当前值比例算。
约定：只往高了补，不往低了压。
当前 20 刀、目标 30 刀 → 会执行，改成 30 刀。
当前 50 刀、目标 30 刀 → 不执行，保持 50 刀（避免误伤正在跑的广告）。
若配置了「最高预算」，设完不会超过该上限。
小结：
加钱有「天花板」，减钱有「地板」，设预算只补不压。
运营和老板汇报时可以说：系统不会把预算调到超出你设的范围，也不会用「设置预算」把高预算广告强行拉低。
7. 执行频率（对「每个广告」的节奏）
是什么：
对每一个广告来说，「这条规则」两次动手之间，至少要隔多久。
不是「整条规则多久跑一次」，而是「每个广告被这条规则动一次后，要隔 N 分钟才能再动一次」。
可选：
15 分钟、30 分钟、1 小时、3 小时、6 小时，或自定义 X 小时。
默认一般是 15 分钟。
举例：
规则 R 管 100 条广告，执行频率 1 小时。
10 条在 9:00 触发了 → 这 10 条在 10:00 之前不会再被这条规则动。
另外 90 条如果 9:30 才满足条件 → 9:30 就可以被这条规则动，不受那 10 条影响。
对你：
想少动一点、避免频繁调价：选 1 小时或更长。
想反应快一点：选 15 分钟或 30 分钟。
8. 执行时间（每天几点到几点允许动）
是什么：
只在你设定的时间段内，规则才会真正执行动作（暂停、开启、改预算）；时间段外即使条件满足，也不会动手，只记一条「不在执行时间段」的跳过记录。
配置：
全天：不设时间段，24 小时都可能执行。
指定时间段：例如「09:00–18:00」或「09:00–18:00 和 21:00–23:30」（北京时间）。
用途：
避免半夜自动改广告，方便第二天看数据再人工判断。
只在上班时间或投放团队在线时自动执行，便于盯盘。
注意：
时间按北京时间算。
若当前不在任何一段内，规则仍会「检查」并占一次冷却，只是不执行动作（避免每分钟都重试）。
四、系统自动在后台做什么？（定时任务大致节奏）
给老板/运营汇报时，可以这样说：
规则检查：每分钟跑一遍，看哪些账户下哪些广告满足哪条规则；满足的再按「执行频率」「执行时间」决定是否真的动手。
所以：条件满足了，最多等 1 分钟就会进入判断；是否动手还看「每广告间隔」和「时间段」。
数据同步：每 15 分钟做一次统一数据同步（今日/近几天数据），保证规则判断用的是较新的花费、ROAS 等。
其他：还有按小时的账户列表同步、结构同步、每天的热表清理等，主要是保证数据新、系统稳；日常使用不用记细节。
五、和运营/老板汇报时可强调的几点
动态筛选：按条件自动圈定要管的广告，新上的符合条件会自动管，不用天天改规则。
2 秒防抖：改条件或保存规则后，系统会稍微等一下再刷新，避免卡顿和误触。
模板：常用策略做成模板，新人也能快速建规则；管理员可维护模板。
排除名单：重要广告可以单独排除，不被某条规则动。
执行优先级：同一条广告多条规则冲突时，优先「暂停」再「减预算」再「加预算」，避免系统在亏钱广告上还加钱。
预算有上下限：加钱有封顶、减钱有保底、设预算只补不压，不会乱调。
执行频率：按「每个广告」间隔控制，不会对同一广告频繁操作。
执行时间：可限制在上班时段执行，半夜不会自动改广告。