🚀 FB广告逻辑引擎 - 系统升级与功能使用说明书
版本： 2026-03-07 Release
核心成果： 实现了动态筛选的多账户闭环、同步速度提升10倍、预算调整绝对安全。
第一部分：核心功能升级（业务价值）
1. 🎯 动态筛选：多账户闭环与快照化 (Dynamic Filtering Loop)
> 解决痛点： 以前规则只能跑单账户，且每次执行都要现场算广告，慢且不稳定。现在支持一条规则跨多个账户，且“所见即所得”。
功能原理：
快照机制 (Snapshots)： 系统不再在执行那一刻去计算“哪些广告叫 US_ 开头”，而是提前算好存入数据库表 (rule_matched_objects)。执行时直接读取，毫秒级响应。
多账户隔离： 一条规则如果选了 A、B 两个账户，系统会分别为 A 和 B 生成独立的快照。A 账户爆了（超过1000条）不会影响 B 账户的正常执行。
逻辑公式： 最终执行对象 = (动态筛选命中 ∪ 手动指定) - 排除名单。
业务价值：
稳定性： 即使 FB 接口挂了，只要快照还在，规则就能继续执行（使用最近一次成功的快照）。
一致性： 规则保存后立即触发计算（Trigger B），你在界面上看到的“匹配数量”就是系统即将执行的数量。
2. ⚡ 极速同步：Track 2 架构 (Fast Track Sync)
> 解决痛点： 以前同步一次全量数据需要很久，导致规则反应迟钝。
功能原理：
双轨制：
Track 1 (兜底)： 每小时慢慢跑，确保数据绝对完整。
Track 2 (极速)： 每20分钟跑一次，只抓取“最近有变化”的广告（增量同步）。
全局限流： 引入了全局水闸（Global Limiter），无论开启多少个任务，发往 FB 的请求永远不会超过设定的安全阈值，防止封号或报错。
业务价值：
时效性： 新建的广告，最快 20 分钟内就能被系统捕获并开始跑规则（以前可能要 1 小时）。
安全性： 大幅降低 API 调用量，保护账户配额。
3. 🛡️ 预算护栏：Pre-Flight 预检与冷却 (Budget Guardrails)
> 解决痛点： 以前系统可能会反复尝试修改预算，或者把预算改得过高/过低。
功能原理：
Pre-Flight (起飞前检查)： 在发指令给 FB 之前，先看一眼当前预算。如果 当前预算 == 目标预算，系统会跳过 API 调用，但标记为“已执行”。
严格语义：
Increase：涨预算，但绝不超过上限（Cap）。
Decrease：降预算，但绝不低于下限（Floor）。
Set：设置预算，只允许上调（防止误伤正在跑量的广告）。
业务价值：
防抖动： 避免了“系统觉得要改 -> 改了 -> 下次检测又觉得要改”的死循环。
省配额： 无效的 API 请求被拦截在本地，不再浪费 FB 配额。
4. 🌍 时区与日志统一 (Timezone Normalization)
> 解决痛点： 以前日志里的时间总比现在慢 8 小时，查问题头晕。
功能原理： 数据库统一存 UTC（世界标准时间），前端统一转北京时间展示。
业务价值： “所见即所得”，日志时间与你电脑右下角时间完全一致。
第二部分：操作与教学指南（团队培训）
请向团队成员（优化师/运营）传达以下操作规范：
1. 如何配置“动态筛选”规则？
场景： 我想管理所有名字里带 US_Winter 的广告。
操作步骤：
在规则编辑页，开启 "使用动态筛选" 开关。
筛选条件： 输入 名称 包含 US_Winter。
排除名单（新功能）： 如果有几个特定的广告不想跑，直接在“排除对象”里把它们的 ID 粘贴进去（支持 act_xxx:id 格式）。
保存反馈： 保存后，请留意规则列表卡片。
🟢 绿色 Normal： 正常，鼠标悬停可看到匹配了多少条。
🔴 红色 Error： 提示“匹配超过上限（默认1000条）”。教学点： 这时必须去收窄条件，否则该账户的这条规则不会执行。
手动刷新： 如果你刚改了广告名，想立刻让规则生效，点击卡片上的 "刷新范围" 按钮。
2. 预算规则的“三不”原则
告诉团队，现在的预算规则非常智能，但也更严格：
Increase (加预算)： 必须填上限。系统承诺： 无论怎么加，绝不会超过这个上限。
Decrease (减预算)： 必须填下限。系统承诺： 无论怎么减，绝不会低于这个下限。
Set (设预算)： 注意： 这个动作现在只升不降。
例子： 目标设为 $50。如果广告当前是 $20，系统会提到 $50。如果当前已经是 $80，系统不会把它降回 $50（为了保护高消耗广告）。
3. 关于“冷却时间”的理解
现象： “我设置了每 2 小时加一次预算，为什么日志显示‘执行了’，但 FB 后台预算没变？”
解释： 这是系统的 Pre-Flight 保护。
系统检测到当前预算已经是你想要的值了（或者是达到了上限）。
为了保护账号，系统跳过了这次修改。
关键点： 虽然没改，但冷却时间依然会被重置。这意味着系统会等下一个 2 小时再来检查，而不是每分钟都来骚扰这个广告。
4. 遇到问题怎么查？（日志教学）
让团队多看 "系统日志" (Logs) 页面。
Trigger B： 看到这个词，说明是你刚保存规则触发的刷新。
Trigger C： 看到这个词，说明是你手动点的刷新。
Skipped (Pre-check)： 说明系统认为没必要改，是正常的省钱行为。
📋 总结：给老板的汇报话术
> “老板，这次升级我们完成了逻辑引擎的闭环改造。
>
> 1. 效率上，同步速度从小时级提升到了 20分钟级。
> 2. 能力上，支持了跨账户的批量动态管理，以前要配10条规则，现在1条搞定。
> 3. 安全上，加了预算护栏和全局限流，杜绝了系统把预算改爆或把账号跑挂的风险。
>
> 现在系统已经具备了大规模自动化投放的基石，后续我们可以放心增加更多规则了。”