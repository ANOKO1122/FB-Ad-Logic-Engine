第一阶段：云端“地基”与“门禁”改造（在云服务器做）
目标： 将 FB 系统移至 80 端口，统一数据库名称，并装上钉钉扫码锁。
有理有据： 端口和域名是服务器环境特有的，本地无法模拟。先定好“大门”，后续开发才有依据。
准备阶段（钉钉授权）：
动作：去钉钉开放平台拿到 AppKey 和 AppSecret。
细节：回调地址务必填 http://ad_tools.thinkpro.top/oauth2/callback。
基础设施调整（Docker 编排）：
动作：修改 docker-compose.yml。
细节：
统一 MySQL 容器名为 `mysql`（Compose `container_name`；数据卷 `mysql_data` 不变，非整库迁移）。
把 FB 系统的容器端口映射改为 80:80（占据主入口）。
加入 oauth2-proxy 镜像，填入钉钉参数。
网关策略调整（Nginx 配置）：
动作：修改 Nginx 配置文件。
细节：配置 auth_request 拦截。设置 cookie_domain=.thinkpro.top。
有理有据：设置根域名 Cookie，才能实现“80 端口扫码一次，8081 端口同步免登”。
云端固化与反向同步：
动作：执行 git push。
有理有据：把这套生产环境的“标准地基”存入 GitHub，防止本地代码以后把它覆盖掉。
第二阶段：同步“蓝图”到本地（连接云端与本地）
目标： 让本地 Cursor 知道云端的最新变化。
有理有据： 只有本地和云端的 Nginx 路径认识一致，你写的“跳转按钮”链接才不会失效。
本地拉取：
动作：在本地 Cursor 终端执行 git pull。
环境变量对齐：
细节：检查本地 .env。确保 DB_HOST 依然是 localhost（本地连本地库），但你要知道云端的数据库容器名已统一为 `mysql`。
第三阶段：本地业务功能开发（在本地电脑做）
目标： 增加“删除用户”和“跳转按钮”。
有理有据： 业务逻辑修改需要频繁调试，在本地写完后“一键发布”最专业。
开发“传送门”按钮：
细节：在 FB 系统 Vue 源码里加按钮，链接为 http://ad_tools.thinkpro.top:8081，属性加 target="_blank"。
有理有据：target="_blank" 满足你“弹出新页面”的需求，不中断当前操作。
开发“账户管理”功能：
前端：做一个管理员页面，列出用户，加红色“删除”按钮。
后端 API：增加 DELETE 接口。
SQL 细节：
DELETE FROM users WHERE id = ?
DELETE FROM rules WHERE creator_id = ?（只删创建的，不删编辑的，这是你要求的严谨逻辑）。
提交代码：
动作：本地 git push 到 GitHub。
第四阶段：云端最终部署与验收（回到云服务器）
目标： 让新功能在云端正式上线。
有理有据： 代码已经过本地验证，现在只需在云端执行“生产环境构建”。
拉取并构建：
动作：云端 git pull -> npm run build。
重启生效：
动作：重启 FB 系统服务。
终极验收：
检查点 1：输入域名，是否跳出钉钉扫码？
检查点 2：扫码进入 FB 系统，点击按钮是否能弹出 8081 的融合平台（且不用再次扫码）？
检查点 3：管理员能否成功删除一个测试账户及其创建的规则？