# 下一步工作计划

> **更新时间**：2026-01-21  
> **当前状态**：阶段6完成、滑动窗口语义修复、增强日志统计、TLS监控完善  
> **完成度**：数据架构层（M2）95%，规则配置层（M3）80%

---

## ✅ 刚刚完成的工作（本次窗口）

### 1. 修复 optimizeQuota 参数缺失
- ✅ 修复 `syncAllAccountsSlidingWindow` 函数缺少参数导致的运行时错误
- ✅ 创建测试脚本验证修复

### 2. 增强日志统计
- ✅ 明确区分"成功且有数据"、"成功但无数据"、"失败"三种情况
- ✅ 新增失败账户详情输出
- ✅ 新增无数据账户过多提示

### 3. TLS 错误监控完善
- ✅ 所有 TLS 错误都被记录到监控系统
- ✅ 成功请求也被记录（确保统计准确）
- ✅ 错误率超过 30% 时自动警告

---

## 🔍 需要立即验证的内容（优先级 1）

### 1. 验证增强日志统计效果

**验证步骤**：
```bash
# 1. 启动服务器
npm run dev:server

# 2. 等待下一次定时任务执行（10 分钟内）
# 或手动触发同步测试

# 3. 观察日志输出，应该看到：
# ✅ 所有账户同步完成，共 X 个账户
# 📊 详细统计:
#    - 成功且有数据: X 个
#    - 成功但无数据: X 个
#    - 失败: X 个
```

**验证要点**：
- ✅ 日志能清楚区分三种情况
- ✅ 如果有失败账户，会列出详细信息
- ✅ 如果无数据账户过多，会给出提示

---

### 2. 验证 TLS 错误监控

**验证步骤**：
```bash
# 1. 观察日志，看是否有 TLS 错误
# 应该看到：❌ TLS连接错误: ...

# 2. 如果错误率超过 30%，应该看到警告：
# ⚠️  TLS 错误率过高警告
# 📊 统计信息: ...
```

**验证要点**：
- ✅ TLS 错误被正确记录
- ✅ 成功请求也被记录
- ✅ 错误率统计准确
- ✅ 超过阈值时能及时警告

---

### 3. 验证 today 查询正确性

**验证步骤**：
```bash
# 执行 SQL 验证脚本
mysql -u root -p fb_ad_brain < 验证today查询正确性.sql
```

**验证要点**：
- ✅ `today` 查询只返回 today 的真实数据
- ✅ 不包含 `last_7d` 聚合值
- ✅ 数据时区正确

---

### 4. 验证滑动窗口同步

**验证步骤**：
```bash
# 1. 观察定时任务日志（每 30 分钟执行一次）
# 或手动触发测试：
node test-sliding-window-fix.js

# 2. 执行 SQL 验证
mysql -u root -p fb_ad_brain < 验证滑动窗口同步.sql
```

**验证要点**：
- ✅ `today` 数据只写入 `ad_snapshots`
- ✅ 按日数据更新到 `daily_stats`
- ✅ 迟到归因修复到正确的自然日

---

### 5. 验证冷数据归档

**验证步骤**：
```bash
# 1. 观察定时任务日志（每 10 分钟检查一次）
# 2. 等待账户本地时区的 06:00-06:09 窗口
# 3. 检查 daily_stats 表是否有新数据
```

**验证要点**：
- ✅ 高频检查正常工作
- ✅ 账户本地时区窗口判断正确
- ✅ 幂等保护生效（不会重复归档）

---

### 6. 观察系统运行 24-48 小时

**观察内容**：
- ✅ 定时任务是否正常执行（无崩溃、无错误）
- ✅ 数据同步是否正常（有数据写入）
- ✅ 没有时区不匹配警告
- ✅ TLS 错误率是否在合理范围（< 30%）
- ✅ 增强日志统计是否准确

**建议**：
- 每天检查一次日志
- 记录异常情况
- 如果发现问题，及时处理

---

## 🚀 下一步要开始的工作

### 优先级 1：验证与监控（立即执行，今天）

**目标**：确保系统稳定运行，验证所有功能正常

**任务清单**：
1. ✅ 验证增强日志统计效果
2. ✅ 验证 TLS 错误监控
3. ⚠️ 验证 today 查询正确性（执行 SQL）
4. ⚠️ 验证滑动窗口同步（观察日志）
5. ⚠️ 验证冷数据归档（观察日志）
6. ⚠️ 观察系统运行 24-48 小时

**预计时间**：1-2 天

---

### 优先级 2：前端集成（本周）

**目标**：完成前端规则配置改造，让用户能在界面上配置规则

**任务清单**（根据 `TASKS.md` 第 2.3 节）：

#### 2.1 前端时区转换
- [ ] 创建 `src/utils/timezone.js` 工具函数
- [ ] 所有时间显示统一转换为北京时间（`Asia/Shanghai`）
- [ ] 在数据展示组件中使用时区转换

**预计时间**：1-2 天

#### 2.2 前端规则配置改造
- [ ] 支持多条件配置（metric/operator/value/time_window）
- [ ] 支持逻辑运算符（AND/OR）
- [ ] 增加预设 JSON 模板（止损、扩量）
- [ ] 增加免责提示文案

**预计时间**：3-5 天

**关键文件**：
- `src/views/RuleManager.vue` - 规则管理页面
- `src/services/ruleService.js` - 规则服务

---

### 优先级 3：动作执行层（下周）

**目标**：实现规则匹配后的动作执行逻辑

**任务清单**（根据 `TASKS.md` 第 3 节）：

#### 3.1 动作模型与优先级实现
- [ ] 定义动作配置结构（存储在 `rules.actions` JSON 字段）
- [ ] 实现动作优先级体系（PAUSE > DECREASE_* > INCREASE_*）
- [ ] 实现动作冲突仲裁（同一广告只执行最高优先级动作）
- [ ] 编写单元测试

**预计时间**：3-5 天

#### 3.2 预算护栏与 Pre-Flight 检查
- [ ] 加预算类动作要求 `max_daily_budget` 参数
- [ ] 执行前检查广告状态（避免重复操作）
- [ ] 实现动作队列重试机制（最多 3 次，指数退避）
- [ ] 编写测试用例

**预计时间**：2-3 天

**关键文件**：
- `server/services/actionService.js`（需要创建）
- `server/index.js`（RuleEngine 类需要扩展）

---

### 优先级 4：反馈与监控层（中期）

**目标**：完善审计日志、Dry Run、IM 通知、健康看板

**任务清单**（根据 `TASKS.md` 第 4 节）：

#### 4.1 审计日志与 Dry Run
- [ ] 完善 `automation_logs` 表结构
- [ ] 在规则执行路径中接入日志记录
- [ ] 支持 `is_simulation`（Dry Run 模式）
- [ ] 编写测试

**预计时间**：2-3 天

#### 4.2 IM 机器人与报警去噪
- [ ] 实现 IM 通知模块（钉钉/飞书 Webhook）
- [ ] 实现 60 秒聚合通知
- [ ] 在关键场景发送告警
- [ ] 编写测试

**预计时间**：2-3 天

#### 4.3 系统健康看板 API
- [ ] 实现 `GET /api/system/health` 接口
- [ ] 返回同步状态、规则执行耗时、动作队列状态等
- [ ] 前端健康视图（可选）
- [ ] 编写测试

**预计时间**：1-2 天

---

## 📋 当前项目完成度

| 模块 | 完成度 | 说明 |
|------|--------|------|
| **数据架构层（M2）** | ✅ 95% | 核心功能已完成，时区修复完成，日志统计增强 |
| **规则配置层（M3）** | ✅ 80% | 后端完成，前端待完成 |
| **动作执行层（M4）** | ⚠️ 0% | 待开始 |
| **反馈与监控层（M5）** | ⚠️ 0% | 待开始 |
| **总体完成度** | **约 60%** | - |

---

## 🎯 建议的执行顺序

### 第一阶段：验证与稳定（1-2 天）

1. **立即执行验证**：
   - ✅ 验证增强日志统计
   - ✅ 验证 TLS 错误监控
   - ⚠️ 验证 today 查询正确性
   - ⚠️ 验证滑动窗口同步
   - ⚠️ 验证冷数据归档

2. **观察系统运行**：
   - 运行 24-48 小时
   - 检查日志，确认无异常
   - 记录发现的问题

---

### 第二阶段：前端集成（本周，3-5 天）

1. **前端时区转换**（1-2 天）
   - 创建时区工具函数
   - 在数据展示组件中使用

2. **前端规则配置改造**（3-5 天）
   - 支持多条件配置
   - 支持 AND/OR 逻辑
   - 预设模板

**为什么先做前端**：
- 后端规则引擎已完成，前端是用户直接使用的界面
- 可以让用户开始配置规则，即使动作执行层还没完成
- 前端相对独立，不影响后端稳定性

---

### 第三阶段：动作执行层（下周，5-8 天）

1. **动作模型与优先级**（3-5 天）
2. **预算护栏与 Pre-Flight**（2-3 天）

**为什么放在前端之后**：
- 前端完成后，用户可以配置规则
- 动作执行层是核心功能，需要充分测试
- 需要 Dry Run 模式支持，避免误操作

---

### 第四阶段：反馈与监控层（中期，5-8 天）

1. **审计日志与 Dry Run**（2-3 天）
2. **IM 通知**（2-3 天）
3. **健康看板 API**（1-2 天）

**为什么放在最后**：
- 这些是增强功能，不影响核心流程
- 可以在动作执行层完成后逐步完善
- 需要系统稳定运行一段时间后，才能更好地设计监控指标

---

## ⚠️ 注意事项

### 1. 验证阶段很重要

**原因**：
- 刚刚完成了重要修复（滑动窗口语义、时区更新、日志统计）
- 需要确保系统稳定运行
- 发现问题要及时修复

**建议**：
- 不要急于开始新功能
- 先观察 24-48 小时
- 确认无问题后再开始前端集成

---

### 2. 前端集成优先级

**为什么先做前端**：
- 后端规则引擎已完成，可以开始使用
- 前端是用户界面，优先级高
- 可以让用户提前熟悉系统

**但要注意**：
- 动作执行层还没完成，前端配置的规则暂时不会执行动作
- 可以先实现 Dry Run 模式，让用户看到规则匹配结果

---

### 3. 动作执行层要谨慎

**原因**：
- 会实际修改 Facebook 广告状态
- 如果逻辑有误，可能造成损失
- 需要充分的测试和 Dry Run 支持

**建议**：
- 先实现 Dry Run 模式
- 充分测试后再开启真实执行
- 添加预算护栏和 Pre-Flight 检查

---

## 📝 验证清单

### 立即验证（今天）

- [ ] 验证增强日志统计效果
- [ ] 验证 TLS 错误监控
- [ ] 执行 SQL 验证 today 查询
- [ ] 观察滑动窗口同步日志
- [ ] 观察冷数据归档日志

### 持续观察（24-48 小时）

- [ ] 定时任务正常执行（无崩溃）
- [ ] 数据同步正常（有数据写入）
- [ ] 没有时区不匹配警告
- [ ] TLS 错误率在合理范围（< 30%）
- [ ] 增强日志统计准确

---

## 🚀 下一步行动

### 今天（立即执行）

1. **执行验证步骤**：
   ```bash
   # 1. 验证 today 查询
   mysql -u root -p fb_ad_brain < 验证today查询正确性.sql
   
   # 2. 验证滑动窗口同步
   mysql -u root -p fb_ad_brain < 验证滑动窗口同步.sql
   
   # 3. 观察定时任务日志
   # 等待下一次执行，检查日志输出
   ```

2. **观察系统运行**：
   - 启动服务器
   - 观察 1-2 小时
   - 记录发现的问题

---

### 本周（前端集成）

1. **前端时区转换**（1-2 天）
   - 创建 `src/utils/timezone.js`
   - 在数据展示组件中使用

2. **前端规则配置改造**（3-5 天）
   - 支持多条件配置
   - 支持 AND/OR 逻辑
   - 预设模板

---

### 下周（动作执行层）

1. **动作模型与优先级**（3-5 天）
2. **预算护栏与 Pre-Flight**（2-3 天）

---

## 📚 相关文档

- **任务清单**：`TASKS.md`
- **开发计划**：`DEV_PLAN.md`
- **快速开始**：`下一个窗口快速开始.md`
- **项目总结**：`项目开发过程/项目总结-2026-01-21-阶段6完成与滑动窗口语义修复.md`
- **定时任务说明**：`定时任务执行时间说明.md`
- **增强日志总结**：`增强日志统计与TLS监控实现总结.md`

---

## ✅ 总结

### 当前状态
- ✅ 数据架构层 95% 完成
- ✅ 规则配置层 80% 完成（后端完成，前端待完成）
- ✅ 刚刚完成：增强日志统计、TLS 监控完善

### 下一步重点
1. **立即**：验证系统稳定性（1-2 天）
2. **本周**：前端集成（3-5 天）
3. **下周**：动作执行层（5-8 天）

### 关键提醒
- ⚠️ 先验证再开发：确保系统稳定后再开始新功能
- ⚠️ 前端优先：让用户能开始使用系统
- ⚠️ 动作执行要谨慎：需要充分测试和 Dry Run 支持

---

**祝你开发顺利！** 🚀

