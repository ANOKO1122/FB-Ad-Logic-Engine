第一部分：方案概括版 (Summary)
1. 数据架构层（基础稳固）
数据搬运工 (Data Ingestor)： 局域网 Node.js 脚本每 10-15 分钟运行一次，将 37 个账户的数据镜像到本地 MySQL。
读写分离逻辑： 监控大屏和规则引擎只读本地数据库；执行动作时才通过集中式动作队列调用 FB API。
集中频率管理： 统一使用一个管理员 Token，通过顺序执行（非并发）和操作缓冲，确保不触发 FB 的 Rate Limit。
数据新鲜度校验 (Staleness Check)： 执行规则前检查同步时间，若数据过旧则熔断，防止基于错误信息的误操作。
2. 规则配置层（颗粒度与友好性）
多层级精细指派： 采用“账户 -> 系列 -> 广告组 -> 广告”层级筛选流，配合状态过滤器，实现精准规则绑定。
复合逻辑判断 (AND/OR)： 支持多指标组合判断（如：ROAS 低且消耗高），实现复杂决策。
灵活时间窗口： 支持今天、昨天、过去 3 天、累计至今等多种维度，平衡波动与趋势。
SOP 模板化： 预设行业成熟的止损、扩量 JSON 模板，运营一键填空即可使用。
3. 动作执行层（安全与可控）
用户自定义冷却期： 允许用户设置操作频率，防止短时间内重复加价或开关，确保策略平稳。
预算安全护栏： 自动扩量时强制设置金额封顶，防止因算法或意外导致的预算失控。
Dry Run (模拟运行)： 支持“只记录不执行”模式，让运营在实测前验证逻辑安全性。
异常处理机制： 针对除零、空值、API 失败进行专项优化，保证引擎运行不中断。
4. 反馈与监控层（透明度）
详细执行日志： 全程记录审计日志，让每一项自动操作都有据可查。
IM 机器人通知： 实时对接钉钉/飞书/企微，重要变动自动 @负责人，实现“闭环管理”。
系统健康看板： 开发者视角的可视化监控，实时掌握搬运工和规则引擎的运行状态。
第二部分：详细拆解版 (Detailed Implementation)
1. 数据架构层
数据搬运工 (Data Ingestor)
逻辑： 解决“多人同时查 API 导致崩溃”和“页面加载慢”的问题。
做法： 编写一个独立的 Node.js 进程，利用 node-cron 设定每 10 分钟循环一次。通过管理员 Token 依次拉取 37 个账户的 insights（指标）和 ads（状态），使用 ON DUPLICATE KEY UPDATE 写入本地 MySQL 镜像表。
意义： 将系统压力从 Facebook 侧转移到本地数据库。运营在看盘和规则引擎扫描时，都是毫秒级响应，且不消耗任何 FB API 额度。
读写分离逻辑
逻辑： 解耦“查询”与“操作”，防止规则扫描影响系统性能。
做法： 所有的 UI 界面、报表、规则判断条件全部连接本地 MySQL 镜像表；只有在规则触发 pause_ad 或 update_budget 等修改指令时，才调用 API。
意义： 极大降低了 API 频率消耗，因为 99% 的规则检查结果都是“不满足条件”，这些无效检查不应该消耗宝贵的 API 频率。
集中频率管理
逻辑： 全公司共用一个 Token，必须建立“排队机制”，防止 API 爆表导致全线瘫痪。
做法：
A. 串行执行： 数据搬运时，弃用 Promise.all()，改用 for...of 循环，一个账户处理完再处理下一个。
B. 动作队列： 所有手动或自动的修改操作存入 api_tasks 表，由 Worker 进程按序取出。
C. 合并 Batch： 同一账户下多个关停操作合并为一个 Batch 请求。
D. 硬限制： 底层代码锁定全系统每秒 API 请求数 ≤ 5 次。
意义： 确保即便 15 个运营同时操作，系统也能像食堂窗口一样有序处理，保证全局稳定性。
数据新鲜度校验 (Data Staleness Check)
逻辑： 假设搬运工因为网络问题断了，本地数据会停留在 1 小时前，此时执行规则会导致误判。
做法： 规则引擎在每一轮扫描开始前，先执行 SELECT MAX(last_sync_time) FROM ad_snapshots。如果 当前时间 - last_sync_time > 30分钟，立即停止执行。
意义： 它是系统的“自动熔断器”，确保所有自动决策都是基于新鲜、准确的数据，防止因信息滞后导致的资产损失。
2. 规则配置层
多层级精细指派
逻辑： 解决规则“误伤”问题，让运营能指定具体的管理对象。
做法： 前端设计级联选择器：用户选定账户后，异步加载该账户下的系列 -> 组 -> 广告。配合“运行中”过滤器，将选中的 ID 存入 target_ids。
意义： 实现了“谁投的广告谁管”，运营可以为新上的测试广告设置激进规则，而不影响老牌稳产广告。
复合逻辑判断 (AND/OR)
逻辑： 真实决策通常是多维度的（例如：不仅要消耗高，还要转化低）。
做法： 数据库存储 JSON 格式的条件数组。逻辑引擎在评估时，支持配置 logical_operator: 'AND' | 'OR'。代码层面使用 .every() 或 .some() 进行判定。
意义： 提高了自动化的“智商”，支持如“（消耗 > 20 且 转化 = 0）或（ROI < 1 且 消耗 > 50）”这类专业逻辑。
灵活时间窗口
逻辑： 广告数据有波动，单看今天可能不准。
做法： 在创建规则时，允许用户为条件选择 time_range（如 Today, Yesterday, Last 3 Days）。
意义： 平衡了“实时止损”与“长周期扩量”的需求。止损通常看今天，而扩量（增加预算）通常需要参考过去 3 天的平均表现。
SOP 模板化
逻辑： 降低运营的使用成本和配置错误率。
做法： 提供后端定义的 JSON 模板。运营点击“快速中止规则”模板，系统自动填充 Spend > 20 和 Conversions < 1 的默认值。
意义： 实现了公司投放经验的沉淀，小白运营也能一键套用大手的风控逻辑。
3. 动作执行层
用户自定义冷却期
逻辑： 防止系统在同一时间内疯狂操作（例如：15 分钟加一次预算，一小时加了四次）。
做法： 运营在设置规则时，自定义 cool_down_hours（如 24 小时）。系统每次成功执行动作后，在 execution_logs 标记时间，下次触发前进行对比。
意义： 给广告算法留出稳定的学习时间，符合 Facebook 广告系统的运作逻辑（不建议频繁大幅调整预算）。
预算安全护栏
逻辑： 预防极端情况（如数据错误或逻辑漏洞）导致的账户烧空。
做法： 凡是涉及“增加预算”的动作，前端强制要求填写 max_daily_budget。当计算出的新预算超过此值时，系统强制取最大值或停止操作并报警。
意义： 它是公司的“钱袋子锁”，提供了最后一道物理防御。
Dry Run (模拟运行)
逻辑： 建立运营对自动化系统的信心，防止“新规则上线即灾难”。
做法： 在 rules 表加一个 is_simulation 字段。开启后，判定成功时仅记录 status: 'would_have_executed' 到日志，并通知运营，但不下发 API 指令。
意义： 允许运营在不花钱的情况下测试规则的准确性，是系统推广初期的关键功能。
异常处理机制
逻辑： 保证系统在遇到数据残缺或 API 波动时不崩溃。
做法：
除零保护： if (clicks === 0) ctr = 0。
空值检查： 处理 FB API 可能返回的 null。
API 重试： 针对网络抖动，动作队列支持最多 3 次指数退避重试。
意义： 保证了系统的健壮性，不会因为一个账户的异常导致整个搬运工或规则引擎挂掉。
4. 反馈与监控层
详细执行日志
逻辑： 解决“为什么我的广告被关了”的疑问。
做法： 建立 automation_logs 表，记录：触发时间、广告名称/ID、触发时的具体数据（如：当时消耗 $25.3）、执行的操作、API 返回结果。
意义： 完整的审计追踪。既方便运营回溯操作，也方便开发者排查逻辑 Bug。
IM 机器人通知
逻辑： 自动化不代表不需要人关注，关键变动必须推送到人。
做法： 动作队列执行成功后，调用钉钉/飞书的机器人 Webhook。消息中包含负责人字段，利用机器人的关键词语法实现 @负责人 提醒。
意义： 实现了“无人值守，有事通报”。运营不需要盯着系统看，只有收到手机推送时才需要介入。
系统健康看板
逻辑： 开发者需要一眼看出自动化工厂是否在运转。
做法： 简单的仪表盘页面，显示：
各账户上次同步成功的时间。
规则引擎上一轮扫描的总耗时。
当前动作队列中积压的任务数。
意义： 预防由于局域网断网、代理失效或 Token 过期导致的“静默失败”，确保系统永远在线。

时区处理工具：Luxon (替代 Moment.js)
核心目的：解决服务器时间与广告账户时区不一致导致的“今日数据”计算错误。
选型：使用 Luxon (npm install luxon)。
实现规范：
数据库：在 accounts 表中存储 Facebook 返回的 timezone_name（如 Asia/Shanghai）。
代码逻辑：所有涉及日期的计算，禁止使用 new Date() 或服务器本地时间。

数据冷热分离策略 (Hot/Cold Data ETL)
核心目的：在单 Token 限制下平衡实时性与查询速度。
热数据 (Hot Data - Today)：
更新频率：每 15 分钟轮询一次。
范围：仅拉取“今天”的数据。
理由：Facebook 转化回传本身有延迟，15分钟是 API 额度与数据实效性的最佳平衡点。
冷数据 (Cold Data - History)：
归档时间：每天早上 06:00 (账户当地时间)。
逻辑：将“昨天”的数据汇总写入本地历史表 (daily_stats)。
理由：避开 Facebook 的归因延迟窗口，确保历史数据不再变动，查询历史报表时不消耗任何 API。

Token 智能熔断机制 (Simple Circuit Breaker)
核心目的：防止 Token 失效后系统盲目重试导致 IP 被封，且无需额外部署微服务。
实现逻辑：
全局维护一个计数器 failureCount。
当 API 返回错误码 190 (Invalid OAuth Token) 时：failureCount + 1。
当 failureCount >= 3 时：
触发熔断：将全局状态 isSystemLocked 设为 true。
停止动作：立即停止所有数据搬运和规则扫描任务。
报警：通过 IM 机器人发送最高级别警报。
当 API 请求成功时：重置 failureCount = 0。

ID 精度保护 (ID Precision Protection)
逻辑： 解决“前端查不到广告”的隐形 Bug。Facebook 的广告 ID 长达 17 位以上，而 JavaScript 的 Number 类型在处理超过 16 位的数字时会丢失精度（自动四舍五入），导致后端传给前端的 ID 尾数变成 000，系统因此报错“对象不存在”。
做法：
数据库层：强制将所有 account_id, campaign_id, ad_id 字段设置为 VARCHAR(50)，严禁使用 BIGINT。
传输层：后端引入 json-bigint 库，配置 Axios 的 transformResponse，强制将 API 返回的所有长数字解析为 String 类型，确保 ID 在全链路中以字符串形式流转。
意义： 从根源上消除了因底层数据类型不匹配导致的“幽灵报错”，确保任何广告都能被精确索引和操作。

滑动窗口数据回溯 (Rolling Window Sync)
逻辑： 解决 Facebook 归因延迟导致的数据失真。用户点击广告后可能在 3 天后才购买，如果只同步“今天”的数据，历史日期的转化数会偏低，导致规则引擎误判 ROI 不达标。
做法： 每次同步任务的 time_range 不再仅限于 Today，而是动态设定为 Today + Past 7 Days。利用 MySQL 的 ON DUPLICATE KEY UPDATE 特性，覆盖更新过去一周的数据记录，补齐迟到的转化数据。
意义： 确保规则引擎基于“最终事实”而非“初步数据”进行决策，大幅提升 CPA 和 ROI 判断的准确性，防止误关潜力广告。

API 竞速熔断 (Promise Race Timeout)
逻辑： 防止单点故障拖垮全局。在串行轮询 37 个账户时，若某个账户因网络原因导致请求一直处于 Pending 状态（无响应也不报错），整个 Node 进程会永久卡死，后续账户将无法更新。
做法： 为所有 FB API 请求封装一个 fetchWithTimeout 函数。利用 Promise.race 机制，设定 45 秒硬性超时。一旦超时，强制中断当前请求，记录错误日志，并自动跳过该账户，继续执行下一个账户的任务。
意义： 赋予系统“自愈能力”。即使个别账户的网络连接断开，搬运工主进程依然能保持流动，确保大盘数据的新鲜度。

报警消息聚合 (Notification Debouncing)
逻辑： 解决“狼来了”导致的报警疲劳。在预算重置或流量波动高峰期，可能瞬间触发几十条规则。如果每条都发推送，运营人员会被消息轰炸，最终导致忽略真正重要的警报。
做法： 建立内存级的“消息缓冲池”。当规则被触发时，不立即发送，而是存入队列并启动 60 秒倒计时。倒计时结束时，将队列中的所有报警汇总为一条“聚合报告”（如：近 1 分钟触发 12 次操作，详情见列表...）发送。
意义： 将碎片化的“噪音”转化为高价值的“简报”，既保证了信息的透明度，又提升了运营人员的阅读体验和响应效率。

智能手动挂起与自定义恢复 (Smart Mute & Custom Resume)
1. 核心逻辑 (Logic)
在数据库 ad_snapshots 或 automation_rules 关联表中增加一个字段：mute_until（挂起至某一时间）。
场景 A：系统自动检测到干预
当搬运工（Ingestor）检测到运营在 FB 后台手动开启了广告，系统自动设置一个默认挂起时间（比如你在后台设置里默认的 2 小时）。
场景 B：运营在你的系统内操作（最强推荐）
当运营在你的监控大屏上点击“开启”按钮时，弹出一个微型气泡或对话框，询问：“已为您开启广告，需要在多久后恢复自动规则监控？”
选项包括：1小时、4小时、次日0点、永久停止规则（直到手动恢复）。
2. 数据库变更 (Schema)
code：
SQL
ALTER TABLE ad_snapshots 
ADD COLUMN mute_until TIMESTAMP NULL, -- 自动化规则在此时间前不生效
ADD COLUMN mute_reason VARCHAR(255);   -- 记录为什么挂起（如：“运营张三手动开启”）
3. 运营操作界面 (UI/UX)
在你的监控大屏（那个表格页面）增加一个小功能：
状态图标： 如果某个广告正处于“人工干预挂起期”，在状态栏显示一个小时钟图标。
悬浮修改： 鼠标移上去显示：“自动化已挂起，将于 14:30 恢复。点击修改恢复时间。”
一键恢复： 运营如果觉得自己观察够了，可以点击“立即恢复监控”，清空 mute_until 字段。
4. 对规则引擎的影响 (Engine Update)
在 ruleEngine.js 扫描广告时，增加一个时间过滤条件：
code：
JavaScript
// 伪代码
if (ad.mute_until && DateTime.now() < ad.mute_until) {
    console.log(`广告 ${ad.ad_id} 处于人工优待期，跳过规则判定`);
    return; // 跳过，不执行任何自动化动作
}
策略灵活性：
有些广告运营觉得“我就看 1 小时就行”，有些觉得“这个素材我要观察一整天”。让他们自己选，系统就变成了真正的辅助工具，而不是束缚。
减少责任推诿：
如果运营自己选了“挂起 8 小时”，结果这期间烧了钱，他是没法怪系统的，因为时间是他自己定的。

 数据一致性 (Double Buffering / Atomic Sync) —— 建议中后期添加
AdsPolar 有吗？ 有。在高并发环境下，它们使用分布式锁或数据版本号。
为什么你的方案需要它？ 你的搬运工（Ingestor）是串行跑 37 个账户。跑完所有账户可能需要 2-3 分钟。
风险： 规则引擎（Rule Engine）在第 1 分钟启动时，前 10 个账户是“新数据”，后 27 个账户是“15分钟前的旧数据”。如果某个规则跨账户对比，逻辑就会乱。
如何简单实现？
在 ad_snapshots 增加一个 sync_session_id (可以使用当前时间戳)。
规则引擎只处理 sync_session_id 等于“最新完整批次”的广告。

“请在 Ingestor 逻辑中实现 20-Batch 聚合引擎：
分块逻辑： 使用 lodash.chunk 或类似方法将所有待更新的广告 ID 按每 20 个 分为一组。
Batch 构造： 按照 Facebook 官方 Batch API 规范，在一个 POST 请求中封装这 20 个子请求。
字段定义： 每个子请求包含我们监控大屏需要的：spend, cpc, ctr, roas, actions 等字段，时间范围设为 today。
异常处理： 必须逐一解析 20 个响应子项。如果其中某个 ad_id 报错，记录日志并跳过，确保剩下的数据正常落库。
频率控制： 每完成一组 20-Batch 请求，根据 x-business-use-case-usage 的反馈进行弹性休眠（sleep）。”

解析 Header： 在每次调用 FB API 的回包里，提取 x-business-use-case-usage 这个 Header。
注意： 这个 Header 是一个字符串，需要 JSON.parse。
提取负载： 找到其中的 call_count 或 total_cputime 百分比。
调整延时：
百分比 < 50%： 设置 currentSleep = 500ms（冲刺）。
百分比 50% - 85%： 设置 currentSleep = 2000ms（标准）。
百分比 > 85%： 设置 currentSleep = 10000ms（安全模式），并触发一个低级预警。

数据抓取：使用 20-Batch 模式串行同步 37 个广告账户，并根据 API 响应头中的 x-business-use-case-usage 动态调整休眠时间。
时区逻辑：引入 Luxon，根据账户 timezone_name 计算当地日期进行规则判定。
规则判定：支持运营手动指派 ID 列表，支持 AND/OR 复合条件，支持 Today/Yesterday 等时间窗口。
操作保护：实现基于 MD5 指纹的幂等性校验；当检测到运营手动干预状态时，自动根据 mute_until 字段挂起规则判定。
质量保证：所有 ID 使用 String 处理，所有计算具备除零保护，所有 FB 请求具备 45 秒超时熔断。”

只需要在【2. 规则配置层】或【3. 动作执行层】加一句话作为**“双重保险”**：
运行时状态校验 (Runtime Status Check)：
无论 target_ids 包含了哪些广告，规则引擎在每次执行扫描时，强制跳过 effective_status 不为 ACTIVE 的对象。
即：10 个广告绑定了规则，若 3 个已关闭，引擎会自动忽略这 3 个，只计算其余 7 个的数据。

前端加一句提示（免责声明）：
在规则设置页面或者系统首页，写一行醒目的字：
⚠️ 注意：本系统严格执行规则。如果您在 FB 后台手动强开已被系统关闭的广告，请务必先在本系统内“暂停”对应规则，否则系统会在下一轮扫描时再次将其关闭。

3. 关于“数据一致性”与“动作执行前的二次校验 (Pre-Flight Check)”
虽然你设计了读写分离，但有一个极端场景：
搬运工 10:00 同步数据（花费 $19）。
规则引擎 10:01 判断（条件：花费 > $20 关停），此时不满足。
现实世界中 10:02 广告花费突增到 $25。
规则引擎要等到 10:15 才能发现。
或者反过来：
搬运工显示数据达标，规则引擎准备加预算。
但在这一分钟内，人工已经在后台把广告关了。
规则引擎如果不检查，可能会把已关停的广告加预算（虽然通常不生效，但会报错）。
优化建议：Pre-Flight Check（飞行前检查）
在执行动作队列（Action Executor）的那一刻，对于修改类操作（改预算、开关），增加一个轻量级的实时检查：
code
JavaScript
// 在执行 "暂停广告" 动作前
async function executePauseAction(adId) {
  // 1. 【飞行前检查】只查 status 字段，极快，消耗极低
  const currentAd = await fbApi.get(`${adId}?fields=status`);
  
  // 2. 如果已经是 PAUSED，则标记任务为“跳过”，不报错
  if (currentAd.status === 'PAUSED') {
    log('系统想关停，但发现已经是关停状态，跳过');
    return;
  }
  
  // 3. 真正执行 POST
  await fbApi.post(`${adId}`, { status: 'PAUSED' });
}
这样可以极大减少 API 的无效报错（Redundant Update）。
4. 数据库 Schema 的最终确认 (Drizzle/SQL)
针对你提到的新字段，确保类型正确：
code
SQL
-- ad_snapshots 表增强
ALTER TABLE ad_snapshots
ADD COLUMN mute_until TIMESTAMP NULL COMMENT '在此时间前忽略规则',
ADD COLUMN mute_reason VARCHAR(100) COMMENT '手动挂起/自动检测干预',
ADD COLUMN last_sync_status VARCHAR(20) DEFAULT 'OK', -- 用于记录该广告上次同步是否成功
ADD COLUMN effective_status VARCHAR(50); -- 建议同步这个字段，区分 ACTIVE 和 CAMPAIGN_PAUSED

【2. 规则配置层 - 多层级精细指派】中提到了：
“配合‘运行中’过滤器，将选中的 ID 存入 target_ids。”
这有一点小隐患：
这句话的意思是：在创建规则的那一刻，你只选了当时是“开启”的广告存进了数据库。
但是，如果今天早上创建规则时这 10 个广告都是开的，中午运营手动关了其中 3 个。此时数据库里的 target_ids 依然记录着 10 个 ID。
如果不做运行时检查，规则引擎跑起来时，还是会去检查那 3 个已关闭的广告。
修正建议：
你不需要大改方案，只需要在【2. 规则配置层】或【3. 动作执行层】加一句话作为**“双重保险”**：
运行时状态校验 (Runtime Status Check)：
无论 target_ids 包含了哪些广告，规则引擎在每次执行扫描时，强制跳过 effective_status 不为 ACTIVE 的对象。
即：10 个广告绑定了规则，若 3 个已关闭，引擎会自动忽略这 3 个，只计算其余 7 个的数据。

插入到 【1. 数据架构层】 -> 【数据搬运工】下方
(这段是为了解决“归因窗口对齐”的问题，确保系统读到的数据和 FB 后台完全一致)
归因窗口强制对齐 (Attribution Window Alignment)
逻辑：解决 FB API 默认归因（如点击后28天）与广告账户实际设置（如点击后7天+浏览后1天）不一致导致的“数据打架”与决策误判。
做法：在数据搬运工调用 /insights 接口拉取报表时，显式附加参数 use_account_attribution_setting=true。
意义：强制系统以广告账户当前的归因设置为准。无论账户设置为“点击7天”还是“浏览1天”，系统抓取到的 CPA/ROAS 数值将与 Facebook Ads Manager 后台显示的数据100% 像素级对齐，彻底消除因数据口径差异导致的规则误杀或漏判。

插入到 【3. 动作执行层】 -> 【预算安全护栏】下方
(这段就是你要求的 AdsPolar 同款优先级逻辑，我把它转化为了可落地的代码逻辑)
动作冲突仲裁与优先级 (Conflict Resolution & Action Priority)
逻辑：解决单次扫描中同一广告命中多条互斥规则（如同时触发“ROI达标扩量”与“库存不足关停”）时的执行冲突，防止“一边踩刹车一边踩油门”。
做法：
预设权重：在代码层硬编码动作优先级常量，风控类 > 降本类 > 扩量类。
优先级 Level 1 (最高)：PAUSE (暂停), ARCHIVE (归档), DELETE (删除)。
优先级 Level 2 (中等)：DECREASE_BUDGET (降预算), DECREASE_BID (降出价)。
优先级 Level 3 (最低)：INCREASE_BUDGET (加预算), INCREASE_BID (加出价)。
竞争择优：规则引擎在生成动作队列前，先按 ad_id 分组。如果同一个广告 ID 产生了多个待执行动作，仅保留优先级最高的一个，直接丢弃低优先级动作。
意义：固化了“安全第一”的决策逻辑。当止损信号出现时，系统会强制忽略任何扩量信号，确保在复杂策略叠加时，系统的行为依然确定、安全且可解释。