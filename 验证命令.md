# AdsPolar 修复验证命令

## 验证目标
1. ✅ 动作B：写入清洗逻辑（过滤 spend=0 且 impressions=0 的僵尸数据）
2. ✅ 动作A：查询口径修改（使用 data_date 而不是 synced_at）
3. ✅ 问题1：验证脚本接受 1% 热数据差异

---

## 步骤1：验证写入清洗逻辑

### 1.1 启动后端服务
```bash
# 在项目根目录执行
npm run dev
# 或者
node server/server.js
```

### 1.2 手动触发一次同步任务（观察清洗日志）
在 Node.js REPL 或后端控制台中执行：
```javascript
import('./server/services/cronService.js').then(m => m.manualUnifiedHeartbeat())
```

**预期输出：**
- 应该看到类似 `🧹 [队列写入器] 数据清洗: X → Y (过滤 Z 条 spend=0 且 impressions=0 的僵尸数据)` 的日志
- 如果没有僵尸数据，则不会显示清洗日志（这是正常的）

### 1.3 验证数据库中没有新的僵尸数据
在 MySQL 中执行：
```sql
-- 检查最近1小时内是否有新的 spend=0 且无交互的数据
-- 注意：ad_snapshots 表没有 impressions 字段，使用 link_clicks 和 purchases 判断
SELECT 
  account_id,
  ad_id,
  spend,
  link_clicks,
  purchases,
  data_date,
  synced_at
FROM ad_snapshots
WHERE synced_at >= DATE_SUB(NOW(), INTERVAL 1 HOUR)
  AND spend = 0
  AND (link_clicks IS NULL OR link_clicks = 0)
  AND (purchases IS NULL OR purchases = 0)
ORDER BY synced_at DESC
LIMIT 20;
```

**预期结果：**
- 如果清洗逻辑正常工作，应该返回 0 行（或只有历史数据，synced_at 是旧的）

---

## 步骤2：验证查询口径修改（data_date）

### 2.1 检查归档查询是否使用 data_date
在 MySQL 中执行：
```sql
-- 查看最近归档的数据，确认 data_date 字段正确
SELECT 
  account_id,
  ad_id,
  data_date,
  DATE(synced_at) as synced_date_utc,
  timezone_name,
  spend
FROM ad_snapshots
WHERE data_date = DATE_SUB(CURDATE(), INTERVAL 1 DAY)
ORDER BY account_id, ad_id
LIMIT 20;
```

**预期结果：**
- 应该能看到昨天的数据（data_date = 昨天）
- synced_date_utc 可能和 data_date 不同（这是正常的，因为有时区差异）

### 2.2 手动触发归档（测试归档查询）
在 Node.js REPL 或后端控制台中执行：
```javascript
import('./server/services/cronService.js').then(m => m.manualArchive())
```

**预期输出：**
- 应该看到归档成功的日志
- 检查 `daily_stats` 表中是否有新数据

---

## 步骤3：验证脚本接受 1% 热数据差异

### 3.1 运行验证脚本
```bash
node server/scripts/verify-spend-ads-completeness.js
```

**预期输出：**
- 如果遗漏率 ≤ 1%，会显示：`✅ 遗漏率 X% ≤ 1%，属于正常的热数据差异（15分钟内会同步）`
- 在结论部分，会区分"真实遗漏"和"热数据差异"
- 如果所有遗漏都是热数据差异，结论会显示：`✅ 结论: 所有遗漏（X 个）都属于正常热数据差异（≤1%），15分钟内会同步，无需处理`

### 3.2 检查验证脚本的详细报告
观察输出中的：
- `遗漏率 X%` 字段
- `isHotDataDiff` 标记
- 结论部分的分类说明

---

## 步骤4：综合验证（SQL 查询）

### 4.1 验证 data_date 查询口径
```sql
-- 查询今天的数据（使用 data_date，而不是 synced_at）
SELECT 
  account_id,
  COUNT(DISTINCT ad_id) as ad_count,
  SUM(spend) as total_spend,
  MAX(data_date) as latest_data_date
FROM ad_snapshots
WHERE data_date = CURDATE()
GROUP BY account_id
ORDER BY account_id
LIMIT 20;
```

### 4.2 验证没有新的僵尸数据
```sql
-- 检查今天是否有 spend=0 且无交互的新数据
-- 注意：ad_snapshots 表没有 impressions 字段，使用 link_clicks 和 purchases 判断
SELECT 
  account_id,
  COUNT(*) as zombie_count
FROM ad_snapshots
WHERE data_date = CURDATE()
  AND spend = 0
  AND (link_clicks IS NULL OR link_clicks = 0)
  AND (purchases IS NULL OR purchases = 0)
GROUP BY account_id
HAVING zombie_count > 0;
```

**或者更简单的验证（只检查 spend=0）：**
```sql
-- 检查今天是否有 spend=0 的数据（更严格的验证）
SELECT 
  account_id,
  COUNT(*) as spend_zero_count
FROM ad_snapshots
WHERE data_date = CURDATE()
  AND spend = 0
GROUP BY account_id
HAVING spend_zero_count > 0;
```

**预期结果：**
- 应该返回 0 行（或只有历史数据，data_date 不是今天）

### 4.3 对比 synced_at 和 data_date 的差异
```sql
-- 查看 synced_at 和 data_date 不一致的情况（历史数据被更新 synced_at）
SELECT 
  account_id,
  ad_id,
  data_date,
  DATE(synced_at) as synced_date_utc,
  spend,
  synced_at
FROM ad_snapshots
WHERE data_date < DATE(synced_at)
  AND synced_at >= DATE_SUB(NOW(), INTERVAL 24 HOUR)
ORDER BY synced_at DESC
LIMIT 20;
```

**预期结果：**
- 这些是历史数据（data_date 是昨天或更早），但 synced_at 被更新了
- 这解释了为什么之前查询 `synced_at >= 24小时前` 会看到 spend=0 的历史数据

---

## 验证检查清单

- [ ] 步骤1：写入清洗逻辑正常工作（日志显示清洗或没有僵尸数据）
- [ ] 步骤2：归档查询使用 data_date（查询正常执行）
- [ ] 步骤3：验证脚本正确识别热数据差异（≤1% 被标记为正常）
- [ ] 步骤4：SQL 验证确认没有新的僵尸数据（data_date = 今天）

---

## 注意事项

1. **时间窗口差异**：如果验证脚本发现遗漏，但遗漏率 ≤ 1%，这是正常的（15分钟内会同步）
2. **历史数据**：`synced_at` 被更新的历史数据（data_date < synced_at）是正常的，因为 `ON DUPLICATE KEY UPDATE` 会更新 `synced_at`
3. **时区差异**：`data_date` 是基于账户时区的自然日，`synced_at` 是服务器时间，两者可能不同

