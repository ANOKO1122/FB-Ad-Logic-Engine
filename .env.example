# --- 服务端口 ---
PORT=3001

# --- 数据库连接（请替换为本地/目标环境配置） ---
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASS=
DB_NAME=fb_ad_brain

# --- Facebook 配置（示例） ---
FACEBOOK_ACCESS_TOKEN=

# --- M1 定时任务总闸 ---
# 默认建议 true：保持云端默认行为不变，避免未配置导致功能回归
# 本地开发若需防止重复触发，请在 .env 中设为 false
ENABLE_CRON=true
