# 定时任务功能实现方案（方案二：独立 scheduled_tasks 表）

> 版本：v3.2
> 日期：2026-06-08
> 状态：✅ 已实施（Phase 1-8 完成，v3.0 多账户执行支持已实现，v3.1 负责人筛选已实现，v3.2 乐观锁+审计日志修复）

### v3.2 乐观锁补全 + 审计日志修复（2026-06-08）

审查发现方案 v2.2 声明的「全部 update 语句增加乐观锁」在 CRUD 路径未实施，且错误路径审计日志缺 objectName。本次修复补全。

| 变更点 | 旧行为 | 新行为 |
|---|---|---|
| **PUT 更新** | `WHERE id=?` 无版本检查 | `WHERE id=? AND version=?` + `SET version=version+1`，冲突返回 409 |
| **PATCH toggle** | 同上，无版本检查 | 同上，冲突返回 409 |
| **updateTaskAfterExecute/Failure/Skip** | version 递增但未检查 affectedRows | 检查 affectedRows=0 时记录 warn（乐观锁冲突） |
| **审计日志失败路径** | `objectName` 在 try 块内声明，catch 块访问不到 → 错误日志缺对象名称 | `objectName` 提升到 try 前声明（`let objectName = null`），catch 块传入 extra |
| **forceExecuteTask 错误路径** | 同上，缺 objectName | 同上修复 |
| **once 任务 nextExecuteAt** | 执行成功后 `computeNextExecuteAt` 仍返回原始 `schedule_at`（因 `task.lastExecutedAt` 尚未更新） | `updateTaskAfterExecute` 对 once 类型直接设 `nextExecuteAt = null` |
| **前端错误消息** | toggle/delete 失败显示 `e.message`（axios 通用 HTTP 状态），看不到服务器具体原因 | 统一用 `e.response?.data?.error \|\| e.message`，优先展示服务器错误详情 |
| **auditAdId 作用域** | 和 objectName 同样的问题：try 内 const 声明，catch 块引用报 `auditAdId is not defined` | 提升到 try 前 `let auditAdId = targetId` 兜底 |
| **once 任务失败重试** | once 类型失败后走指数退避重试（5→10→20分钟），任务保持启用 + 残留 next_execute_at | once 类型失败后 **2 分钟重试 1 次**（共 2 次尝试），第二次仍失败则禁用 |
| **审计日志刷写时机** | 所有任务执行完后一次性批量 INSERT，服务重启中丢日志 | 每个任务完成后立即刷写审计日志（`flushAuditBatch`），防止重启丢日志 |
| **卡片动作参数展示** | 箭头+数值（如 `→ $100`、`↑ 20%`），含义不明 | 完整文字（如 `设置预算到 $100`、`上调 20%`、`上调 $5（上限 $50.00）`） |
| **卡片动态筛选** | 无展示 | 显示「动态筛选：已开启 / 状态正常 / 当前生效 N 个对象」（对齐规则管理） |
| **动态筛选编辑持久化** | 再次打开编辑时 `scopeConditionRows` 重置为默认空行，已保存条件丢失 | 从 `scope_filters` JSON 反向解析恢复到条件行（复用 `buildScopeRowsFromFilters`） |

### v3.1 负责人筛选（2026-06-08）

复用规则管理的负责人筛选模式，定时任务列表支持按 owner 维度过滤。

| 变更点 | 旧行为 | 新行为 |
|---|---|---|
| **列表查询** | 管理员看全部，普通用户只看自己的 | 管理员可多选负责人筛选 + "管理员创建"；普通用户看同 owner 维度所有任务 |
| **前端 UI** | 无负责人筛选 | 增加负责人下拉多选组件（与 RuleManager 同款） |
| **卡片展示** | 不显示负责人 | 管理员可见每张卡片的负责人名称 |
| **API 响应** | 无 isAdmin / ownerName | 返回 isAdmin 字段 + 每条任务补全 ownerName |

### v3.0 多账户执行支持（2026-06-04）

复用规则管理的 `target_account_ids` + `target_by_account` 双字段模式，定时任务从「单账户执行」升级为「多账户批量执行」。

| 变更点 | 旧行为 | 新行为 |
|---|---|---|
| **账户选择** | UI 可多选账户，但提交时只用 `selectedAccountIds[0]` | 提交所有选中账户 + `target_by_account` 按账户分组目标 |
| **执行方式** | 所有 target 在一个 `account_id` 下执行 | 遍历 `targetByAccount` 的每个账户，逐账户解析目标 → 执行 |
| **动态筛选** | `previewDynamicScope([task.accountId])` | `previewDynamicScope([eachAccountId])` 按账户独立解析 |
| **目标映射** | `target_ids` 为扁平数组 | `target_by_account`：`{ "act_1": ["id1"], "act_2": ["id2"] }` |
| **兼容性** | — | 旧数据仍通过 `account_id` + `target_ids` 正常执行，无 breaking change |
| **数据库** | 052/053/054 迁移 | 新增 055 迁移：`target_account_ids` JSON + `target_by_account` JSON |

### v2.2 调度类型扩展 + 审查修复（2026-06-03）

| 变更点 | 旧行为 | 新行为 |
|---|---|---|
| **调度类型** | 仅 once/daily/weekly（cron 后端支持但前端未暴露） | 新增 interval（按间隔重复），cron 前端补全，共 5 种 |
| **interval 调度** | 不支持 | `schedule_at` 格式 `"15m"`/`"2h30m"`，`computeNextExecuteAt` 以上次执行时间+间隔计算下次执行 |
| **动态筛选执行** | `resolveTargetIdsForTask` 仅返回手动 IDs，TODO 未实现 | 接入 `previewDynamicScope` 实时解析 scope_filters，动态+手动合并去重 |
| **乐观锁 version** | 定义但从未使用 | 全部 update 语句增加 `version = version+1` + WHERE version 校验 |
| **死代码清理** | `hasEffectiveScopeFilters` 定义未调用 | 已删除 |
| **前端排除展示** | 卡片不显示排除名单 | 卡片底部提示"⊘ 排除 N 个对象" |
| **Schema 注释** | 缺 interval | 已补全 |

### v2.1 任务管理完善（2026-06-03）

| 变更点 | 旧行为 | 新行为 |
|---|---|---|
| **任务名称** | 无 | `task_name` 字段，用户自定义；写入日志 `rule_name` 列 |
| **动作选择** | 5 种（顺序混乱） | 5 种（与规则管理同序：⏸暂停→▶️启用→💰增加→💸减少→📌设置固定值） |
| **预算上限/下限** | 不支持 | `increase_budget` 支持上限，`decrease_budget` 支持下限，`set_budget` 无需（固定值） |
| **执行时间 UI** | 独立"1.2 执行时间"区块 | 合并到基本信息区，与执行日期同行或紧随其后 |
| **审计日志 run_id** | 一直为 NULL | 每次执行批次自动生成 `sched_{taskId}_{ts}_{random}` |
| **审计日志 object_name** | 一直为 NULL | 从 `structure_ads/adsets/campaigns` 表实时查询 |
| **审计日志 API 交互** | 不记录 | `executeAction` 捕获 `apiRequest`/`apiResponse` 写入日志 |
| **审计日志 rule_name** | 一直为 NULL | 写入 `task.taskName`，日志页可区分任务来源 |
| **数据库** | 053 迁移 | 新增 054 迁移：`task_name` 列 |
| **once 任务 next_execute_at** | 首次创建时错误地设为 NULL | 修复：未执行过的 once 任务正确计算 `schedule_at` 的 UTC 时间 |

### v2.0 多对象支持（2026-06-01）

从规则管理复用动态筛选 + 多选 + 排除名单模式，定时任务从「单对象执行」升级为「多对象批量执行」。
触发条件仍是时间，其余（作用对象定位、范围筛选、排除逻辑）全部复用规则管理的成熟能力。

| 变更点 | 旧行为 | 新行为 |
|---|---|---|
| 目标对象 | 单个 `target_id` | `target_ids` JSON 数组 + `target_id` 向后兼容 |
| 对象选择 UI | radio 单选 | checkbox 多选 + 全选/清空 + 已选标签预览 |
| 动态筛选 | 仅 UI 展示，未接通 API | 完整复用 `previewDynamicScope`，实时 matchedCount |
| 排除名单 | 无 | 完整复用规则管理的排除区（搜索、多选、清空） |
| 执行方式 | 单对象 → executeAction 一次 | `resolveTargetIdsForTask()` → 遍历所有对象逐条执行 |
| 审计日志 | 一个 task 一条日志 | 一个 task × N 个对象 → N 条日志，批量 INSERT |
| 手动执行反馈 | `success: true/false` | `执行完成: {N} 成功, {M} 失败` |
| scope_filters | 前端发送但后端丢弃 | 后端存储 + 校验；执行时参与目标解析 |
| exclude_ids | 不存在 | 新增字段，与规则同格式 `{ ad_ids, adset_ids, campaign_ids }` |
| 数据库 | 052 建表 | 新增 053 迁移：6 个 ALTER COLUMN |

### v1.6 本次变更

`set_budget` 语义从「仅上调」改为「真正的设置预算（可上可下）」。定时任务在 `scheduledTaskService.js` 内直接计算目标美分，**不经过** `computeNewBudgetCentsOnce`（该函数对 `set_budget` 限定为仅上调，是为现有条件规则设计的保守策略，不适用于定时任务场景）。

| 变更点 | 旧行为 | 新行为 |
|---|---|---|
| `set_budget` | 当前预算≥目标 → 跳过不下调 | 直接设为目标值，无论当前更高或更低 |
| 前端文案 |「上调预算到」+ ⚠️仅上调 |「设置预算到」 |
| 计算路径 | 走 `computeNewBudgetCentsOnce`（仅上调） | 在 `executeAction` 内直接 `Math.round(value * 100)` + clamp `$1~$FB上限` |

### v1.1 审查修复清单

| 问题编号 | 严重级别 | 问题描述 | 修复方式 |
|---|---|---|---|
| #1 | 🔴 严重 | campaign 级预算调用了 getAdsetBudget | `executeAction` 按 targetLevel 分流：campaign→`getCampaignBudget`/`updateCampaignBudget` |
| #2 | 🔴 严重 | 失败任务每分钟无限重试 | 新增 `retry_count`/`max_retries` 字段 + 指数退避（5分→10分→20分）+ 超限自动禁用 |
| #3 | 🔴 严重 | node-cron 不防并发重叠 | Cron 入口增加 `scheduledTaskRunning` 互斥锁 |
| #4 | 🔴 严重 | set_budget 仅上调不下调，与用户直觉不符 | **v1.6 已彻底修复**：定时任务不走 `computeNewBudgetCentsOnce`，直接计算目标美分，可上可下 |
| #5 | 🟡 中等 | preflightMode 参数未写入 DB | `writeAuditLog` 中按 `isSimulation` 映射为 `'preflight'` |
| #6 | 🟡 中等 | 失败时 lastStatus 未更新 | `updateTaskAfterFailure` 中更新 `lastStatus='fail'` 或 `'stale'` |
| #7 | 🟡 中等 | cron 类型缺少 calcNextExecuteAt | 引入 `cron-parser`，在 switch 中增加 `cron` case |
| #8 | 🟡 中等 | ad 级预算找不到 adset 时会无限重试 | 结合问题#2：先 skip → 重试退避 → 最终禁用 |
| #11 | 🟢 轻微 | once 任务过期容差未在查询中体现 | 查询条件增加 `next_execute_at >= NOW() - INTERVAL 2 MINUTE` |
| #12 | 🟢 轻微 | 缺少最低预算校验 | API 层校验：预算结果 ≥ 100 cents（$1.00） |

---

## 一、方案选型结论

经过对比分析，**方案二优于方案一**，原因：

| 维度 | 方案一（扩展 rules） | 方案二（独立表） |
|---|---|---|
| 对现有功能影响 | 改动 6 个现有文件，包括最核心的 `executeRulesForAccount` | **零影响**——不改任何现有文件 |
| 对系统性能影响 | scheduled 与 condition 共享账户锁，全量拉取规则 | **隔离**——独立 Cron 不竞争锁，精确索引查询 |
| 回滚安全性 | 需回滚 rules 表 + 6 文件代码 | **删除新表 + 新文件即可** |
| 代码清晰度 | 规则表承载两种完全不同语义 | **职责分离**，各走各路 |

---

## 二、核心设计原则：最大化复用现有接口

**方案二不重写任何执行逻辑**。它只解决"存什么时间执行 + 到时间没到"的问题，实际的 **暂停/激活/调预算** 全部调用现有系统接口。

```
scheduled_tasks 表          ← 新增：存"什么时间做什么"
     │
     ▼
scheduledTaskService.js     ← 新增：时间判断 + 调度
     │
     ├─ 读取账户列表 ──▶ accountSyncService.getAccountsFromDatabase()
     ├─ 读取广告结构 ──▶ structureSyncService.listStructureObjectsFromDb()
     ├─ 执行暂停/激活 ──▶ FacebookMarketingAPI.pauseAd/activateAd (index.js)
     ├─ 执行调整预算 ──▶ FacebookMarketingAPI.updateAdsetBudget (index.js)
     │                   actionExecutorService.computeNewBudgetCentsOnce()
     └─ 写审计日志   ──▶ automation_logs 表（复用 Drizzle insert）
```

---

## 三、数据模型设计

### 3.1 新表 `scheduled_tasks`

```sql
CREATE TABLE scheduled_tasks (
  id              INT AUTO_INCREMENT PRIMARY KEY,
  -- 用户归属
  user_id         INT NOT NULL,
  owner_id        INT NOT NULL DEFAULT 0,

  -- 调度配置
  schedule_type   VARCHAR(16) NOT NULL,       -- once / daily / weekly / cron
  schedule_at     VARCHAR(32) NULL,             -- 格式见 3.2；cron 类型可为 NULL
  schedule_cron   VARCHAR(64) NULL,            -- cron 表达式（仅 cron 类型）
  schedule_timezone VARCHAR(50) NULL,          -- 时区，NULL=跟随账户时区
  next_execute_at DATETIME NULL,               -- 下次执行时间（UTC），由系统计算维护

  -- 目标对象（v2.0 多对象支持）
  account_id      VARCHAR(50) NOT NULL,        -- 广告账户 ID（act_xxx），主账户（向后兼容）
  target_account_ids JSON NULL,                -- v3.0 多选账户ID列表（JSON数组）
  target_by_account JSON NULL,                 -- v3.0 按账户分组的目标ID
  target_level    VARCHAR(16) NOT NULL DEFAULT 'ad',  -- ad / adset / campaign
  target_id       VARCHAR(50) NULL,            -- ⚠️ v2.0 改为可空，向后兼容；新任务用 target_ids
  target_ids      JSON NULL,                   -- v2.0 多目标ID列表（JSON数组，如 ["id1","id2"]）
  use_dynamic_scope TINYINT(1) NOT NULL DEFAULT 0,  -- v2.0 动态筛选开关（复用规则管理的 scope_filters 模式）
  scope_filters   JSON NULL,                   -- v2.0 监控范围条件 { level: 'ad', conditions: [...] }
  exclude_ids     JSON NULL,                   -- v2.0 排除名单 { ad_ids: [...], adset_ids: [...], campaign_ids: [...] }
  max_dynamic_matches INT NOT NULL DEFAULT 1000,  -- v2.0 动态匹配上限

  -- 动作配置
  action_type     VARCHAR(32) NOT NULL,        -- pause_ad / activate_ad / pause_adset / activate_adset / pause_campaign / activate_campaign / set_budget / increase_budget / decrease_budget（五轮审查#2.1：与现有规则系统枚举统一）
  action_params   JSON NOT NULL,               -- 动作参数（见 3.3）

  -- 状态控制
  enabled         TINYINT(1) NOT NULL DEFAULT 1,
  is_simulation   TINYINT(1) NOT NULL DEFAULT 0,  -- Dry Run 模式
  auto_disable    TINYINT(1) NOT NULL DEFAULT 1,  -- once 类型执行后自动 enabled=false

  -- 执行追踪
  last_executed_at DATETIME NULL,
  last_status     VARCHAR(16) NULL,            -- success / fail / skipped

  -- 重试控制
  retry_count     INT NOT NULL DEFAULT 0,      -- 累计重试次数
  max_retries     INT NOT NULL DEFAULT 3,      -- 最大重试次数，超出后自动禁用

  -- 并发控制（五轮审查#2.3）
  version         INT NOT NULL DEFAULT 0,       -- 乐观锁版本号

  created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

  INDEX idx_next_execute (enabled, next_execute_at),
  INDEX idx_account (account_id),
  INDEX idx_user (user_id),
  INDEX idx_owner (owner_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 3.2 `schedule_at` 格式约定

| schedule_type | 格式 | 示例 | 说明 |
|---|---|---|---|
| `once` | `YYYY-MM-DD HH:mm` | `2026-06-03 14:00` | 具体日期时间，执行后自动禁用 |
| `daily` | `HH:mm` | `08:00` | 每天这个时刻 |
| `weekly` | `W1,W2,W3\|HH:mm` | `1,3,5\|09:00` | 周一三五 09:00（W1=周一 ... W7=周日） |
| `interval` | `{h}h{m}m` | `15m` / `2h30m` | 按固定间隔分钟数循环重复执行 |
| `cron` | — | — | 使用 `schedule_cron` 字段，`schedule_at` 可为 NULL |

### 3.3 `action_params` JSON 结构

```json
// ⚠️ 预算单位约定：
//   set_budget 的 value = 美元（scheduledTaskService 内部 ×100 转美分）
//   increase_budget / decrease_budget = 百分比或美元（走 computeNewBudgetCentsOnce）
//   完整链路：value(美元) → ×100 → 美分 → FB API
```

```json
// 动作枚举与现有系统统一（pause_ad / activate_ad / set_budget / increase_budget / decrease_budget）

// 暂停
{ "type": "pause_ad" }

// 激活
{ "type": "activate_ad" }

// 设置预算到 $100（可上可下，v1.6）
{ "type": "set_budget", "value": 100, "max_daily_budget": 50000 }

// 上调 20%
{ "type": "increase_budget", "value": 20, "value_unit": "percent" }

// 下调 $5
{ "type": "decrease_budget", "value": 5, "value_unit": "usd" }
```

### 3.4 完整数据示例

```json
{
  "schedule_type": "once",
  "schedule_at": "2026-06-03 14:00",
  "schedule_timezone": "Asia/Shanghai",
  "account_id": "act_123456789",
  "target_level": "ad",
  "target_id": "123456789012345",
  "action_type": "set_budget",
  "action_params": { "type": "set_budget", "value": 100 },
  "enabled": true,
  "auto_disable": true,
  "is_simulation": false
}
```

### 3.5 Drizzle Schema

```js
// server/db/schema.js 新增
export const scheduledTasks = mysqlTable('scheduled_tasks', {
  id: int('id').primaryKey().autoincrement(),
  userId: int('user_id').notNull(),
  ownerId: int('owner_id').notNull().default(0),
  scheduleType: varchar('schedule_type', { length: 16 }).notNull(),
  scheduleAt: varchar('schedule_at', { length: 32 }),
  scheduleCron: varchar('schedule_cron', { length: 64 }),
  scheduleTimezone: varchar('schedule_timezone', { length: 50 }),
  nextExecuteAt: timestamp('next_execute_at'),
  // 目标对象（v2.0 多对象支持）
  accountId: varchar('account_id', { length: 50 }).notNull(),
  targetLevel: varchar('target_level', { length: 16 }).notNull().default('ad'),
  targetId: varchar('target_id', { length: 50 }),                // v2.0 可空，向后兼容
  targetIds: json('target_ids'),                                  // v2.0 多目标ID数组
  useDynamicScope: boolean('use_dynamic_scope').notNull().default(false),
  scopeFilters: json('scope_filters'),                            // v2.0 监控范围条件
  excludeIds: json('exclude_ids'),                                // v2.0 排除名单
  maxDynamicMatches: int('max_dynamic_matches').notNull().default(1000),
  // 动作配置
  actionType: varchar('action_type', { length: 32 }).notNull(),
  actionParams: json('action_params').notNull(),
  enabled: boolean('enabled').notNull().default(true),
  isSimulation: boolean('is_simulation').notNull().default(false),
  autoDisable: boolean('auto_disable').notNull().default(true),
  lastExecutedAt: timestamp('last_executed_at'),
  lastStatus: varchar('last_status', { length: 16 }),
  retryCount: int('retry_count').notNull().default(0),
  maxRetries: int('max_retries').notNull().default(3),
  version: int('version').notNull().default(0),
  createdAt: timestamp('created_at').notNull().defaultNow(),
  updatedAt: timestamp('updated_at').notNull().defaultNow().onUpdateNow(),
})
```

### 3.6 v2.0 新增迁移 (053)

```sql
-- 053_add_dynamic_scope_to_scheduled_tasks.sql
ALTER TABLE scheduled_tasks MODIFY COLUMN target_id VARCHAR(50) NULL;
ALTER TABLE scheduled_tasks ADD COLUMN target_ids JSON NULL AFTER target_id;
ALTER TABLE scheduled_tasks ADD COLUMN use_dynamic_scope TINYINT(1) NOT NULL DEFAULT 0 AFTER target_ids;
ALTER TABLE scheduled_tasks ADD COLUMN scope_filters JSON NULL AFTER use_dynamic_scope;
ALTER TABLE scheduled_tasks ADD COLUMN exclude_ids JSON NULL AFTER scope_filters;
ALTER TABLE scheduled_tasks ADD COLUMN max_dynamic_matches INT NOT NULL DEFAULT 1000 AFTER exclude_ids;
```

---

## 四、执行流程设计

### 4.1 整体流程（v3.0 多账户 + 多对象版本）

```
每分钟 Cron (* * * * *)
  │
  └─ executeDueScheduledTasks()
       │
       ├─ 1. SELECT * FROM scheduled_tasks
       │      WHERE enabled = 1
       │        AND next_execute_at >= UTC_TIMESTAMP() - INTERVAL 2 MINUTE
       │        AND next_execute_at <= UTC_TIMESTAMP()
       │      LIMIT 100
       │
       ├─ 2. 遍历每条到期任务
       │    │
       │    ├─ getTaskAccountIds(task)  ← v3.0：获取所有目标账户
       │    │   ├─ targetByAccount 有值 → 取其 keys
       │    │   ├─ targetAccountIds 有值 → 用数组
       │    │   └─ 兼容旧 accountId → 单元素
       │    │
       │    ├─ 对每个 accountId  ← v3.0 核心：逐账户循环
       │    │   │
       │    │   ├─ 查 account_mappings 确认账户 active
       │    │   ├─ resolveTargetIdsForTask(task, accountId)
       │    │   │   ├─ targetByAccount[accountId] → 该账户的手动 IDs
       │    │   │   ├─ use_dynamic_scope=1 → previewDynamicScope([accountId])
       │    │   │   └─ 减去 exclude_ids
       │    │   │
       │    │   ├─ 对每个 targetId 执行
       │    │   │   ├─ ad 级预算 → 查 structure_ads 获取 adset_id
       │    │   │   ├─ executeAction(api, task, targetId, budgetObjectId)
       │    │   │   └─ buildAuditRecord → 写入 auditBatch（含 accountId）
       │    │
       │    └─ 整体判定：全部成功→success / 有失败→retry / 全skip→skip
       │
       └─ 5. 批量写入 auditBatch（一次 INSERT N 条）
```

### 4.2 核心函数 `executeDueScheduledTasks`

```js
// server/services/scheduledTaskService.js

// 二次审查 R2#2：修正 logger 导入路径（所有 service 统一使用 ../utils/logger.js）
import logger from '../utils/logger.js'
import { db } from '../db/drizzle.js'
import { scheduledTasks, automationLogs } from '../db/schema.js'
import { and, eq, lte, gte } from 'drizzle-orm'
import pool from '../db/connection.js'
import { FacebookMarketingAPI } from '../index.js'
import { computeNewBudgetCentsOnce } from './actionExecutorService.js'
import { DateTime } from 'luxon'

// 二次审查 R2#4：互斥锁只在此文件定义一次，导出检查函数供 cron 处调用
let _scheduledTaskRunning = false
export function isScheduledTaskRunning() { return _scheduledTaskRunning }

export async function executeDueScheduledTasks() {
  // 二次审查 R2#4：入口处检查互斥锁
  if (_scheduledTaskRunning) {
    return { executed: 0, skipped: 0, errors: 0, skippedDueToLock: true }
  }
  _scheduledTaskRunning = true

  try {
    const token = process.env.FACEBOOK_ACCESS_TOKEN
  if (!token) {
    logger.warn('[ScheduledTask] 跳过：FACEBOOK_ACCESS_TOKEN 缺失')
    return { executed: 0, skipped: 0, errors: 0 }
  }

  // 1. 查询到期的任务（审查问题11：2 分钟容差窗口，避免执行过期太久的 once 任务）
  const toleranceWindow = new Date(Date.now() - 2 * 60 * 1000)
  const dueTasks = await db
    .select()
    .from(scheduledTasks)
    .where(and(
      eq(scheduledTasks.enabled, true),
      gte(scheduledTasks.nextExecuteAt, toleranceWindow),
      lte(scheduledTasks.nextExecuteAt, new Date())
    ))
    .limit(100)

  if (dueTasks.length === 0) return { executed: 0, skipped: 0, errors: 0 }

  // 四轮审查#5：达到上限时告警，避免静默积压
  if (dueTasks.length >= 100) {
    logger.warn('[ScheduledTask Cron] 到期任务数达到 LIMIT 上限 100，可能存在积压')
  }

  const api = new FacebookMarketingAPI(token)
  let executed = 0, skipped = 0, errors = 0

  for (const task of dueTasks) {
    try {
      // 2. 校验账户是否 active
      const [mappingRows] = await pool.execute(
        'SELECT 1 FROM account_mappings WHERE fb_account_id = ? AND is_active = 1 LIMIT 1',
        [task.accountId]
      )
      if (mappingRows.length === 0) {
        logger.info(`[ScheduledTask #${task.id}] 账户 ${task.accountId} 未激活，跳过`)
        skipped++
        await updateTaskAfterSkip(task)
        continue
      }

      // 3. 确定执行目标（审查问题1：按 targetLevel 分流预算操作）
      const targetId = task.targetId
      const isBudgetAction = ['set_budget', 'increase_budget', 'decrease_budget'].includes(task.actionType)
      let budgetObjectId = targetId  // 预算操作的真实对象 ID（adset或campaign）

      if (task.targetLevel === 'ad' && isBudgetAction) {
        // ad 级预算：需查它所属的 adset_id
        const [adRows] = await pool.execute(
          'SELECT adset_id FROM structure_ads WHERE account_id = ? AND ad_id = ? LIMIT 1',
          [task.accountId, targetId]
        )
        if (adRows.length === 0) {
          logger.warn(`[ScheduledTask #${task.id}] 找不到广告 ${targetId}，跳过`)
          skipped++
          await updateTaskAfterSkip(task)
          continue
        }
        budgetObjectId = adRows[0].adset_id
      }

      // 4. 执行动作
      if (task.isSimulation) {
        logger.info(`[ScheduledTask #${task.id}] Dry Run: ${task.actionType} on ${targetId}`)
        executed++
        // 四轮审查#2：先更新状态，后写日志（状态是主数据，日志是追溯数据）
        await updateTaskAfterExecute(task)
        await writeAuditLog(task, 'success', 'preflight', null)
        continue
      }

      await executeAction(api, task, targetId, budgetObjectId)
      executed++
      // 五轮审查#2.2：状态更新 + 日志写入在同一事务中，防止不一致
      await db.transaction(async (tx) => {
        await updateTaskAfterExecute(task)
        await writeAuditLog(task, 'success', null, null)
      })

    } catch (err) {
      logger.error(`[ScheduledTask #${task.id}] 执行失败:`, err.message)
      errors++
      // 四轮审查#2：失败时先更新状态（退避重试），后写日志
      await updateTaskAfterFailure(task)
      await writeAuditLog(task, 'fail', null, err.message)
  }

  return { executed, skipped, errors }
  } finally {
    // 二次审查 R2#4：确保锁一定会释放
    _scheduledTaskRunning = false
  }
}

/**
 * 执行单个动作（审查问题1：按 targetLevel 分别调用正确的 FB API）
 */
async function executeAction(api, task, targetId, budgetObjectId) {
  const { actionType, targetLevel } = task
  const params = typeof task.actionParams === 'string'
    ? JSON.parse(task.actionParams)
    : (task.actionParams || {})

  switch (actionType) {
    case 'pause_ad':
      if (targetLevel === 'adset') return api.pauseAdset(targetId)
      if (targetLevel === 'campaign') return api.pauseCampaign(targetId)
      return api.pauseAd(targetId)

    case 'activate_ad':
      if (targetLevel === 'adset') return api.activateAdset(targetId)
      if (targetLevel === 'campaign') return api.activateCampaign(targetId)
      return api.activateAd(targetId)

    case 'set_budget': {
      // v1.6：set_budget 在定时任务中的语义是"精确设置，可上可下"
      // 不经过 computeNewBudgetCentsOnce（该函数对 set_budget 限制为仅上调）
      const targetUsd = Number(params?.value)
      if (!Number.isFinite(targetUsd) || targetUsd <= 0) {
        logger.warn(`[ScheduledTask #${task.id}] set_budget value 无效: ${params?.value}`)
        return
      }
      let newCents = Math.round(targetUsd * 100)
      newCents = Math.max(newCents, 100) // 最低 $1.00
      if (params?.max_daily_budget != null) {
        const cap = Math.round(Number(params.max_daily_budget))
        if (cap > 0) newCents = Math.min(newCents, cap)
      }
      if (targetLevel === 'campaign') {
        return api.updateCampaignBudget(budgetObjectId, newCents)
      }
      return api.updateAdsetBudget(budgetObjectId, newCents)
    }

    case 'increase_budget':
    case 'decrease_budget': {
      // increase/decrease 仍走 computeNewBudgetCentsOnce（按百分比或美元增减）
      let currentCents
      if (targetLevel === 'campaign') {
        currentCents = await api.getCampaignBudget(budgetObjectId)
      } else {
        currentCents = await api.getAdsetBudget(budgetObjectId)
      }
      const action = { ...params, type: actionType }
      const newCents = computeNewBudgetCentsOnce(currentCents, action)
      if (newCents === currentCents) {
        logger.info(
          `[ScheduledTask #${task.id}] 预算无需调整 ` +
          `（当前=${currentCents}分，目标=${newCents}分，action=${actionType}），跳过API调用`
        )
        return
      }
      if (targetLevel === 'campaign') {
        return api.updateCampaignBudget(budgetObjectId, newCents)
      }
      return api.updateAdsetBudget(budgetObjectId, newCents)
    }

    default:
      throw new Error(`不支持的动作类型: ${actionType}`)
  }
}

/** 计算下次执行时间并更新 */
async function updateTaskAfterExecute(task) {
  const nextAt = await computeNextExecuteAt(task)  // M1: async 函数需 await
  let newEnabled = task.enabled
  if (task.scheduleType === 'once' && task.autoDisable) newEnabled = false

  await db.update(scheduledTasks)
    .set({
      lastExecutedAt: new Date(),
      lastStatus: 'success',
      nextExecuteAt: nextAt,
      enabled: newEnabled,
      retryCount: 0  // 成功后重置重试计数
    })
    .where(eq(scheduledTasks.id, task.id))
}

/**
 * 失败后更新：指数退避重试（审查问题2）
 * 第1次失败：5分钟后重试
 * 第2次失败：10分钟后重试
 * 第3次失败：20分钟后重试（超过 maxRetries → 自动禁用）
 */
async function updateTaskAfterFailure(task) {
  const newRetryCount = (task.retryCount || 0) + 1
  const maxRetries = task.maxRetries || 3
  const now = DateTime.utc()

  if (newRetryCount >= maxRetries) {
    // 超过重试上限，自动禁用
    await db.update(scheduledTasks)
      .set({
        lastStatus: 'stale',
        retryCount: newRetryCount,
        enabled: false,
        lastExecutedAt: now.toJSDate()
      })
      .where(eq(scheduledTasks.id, task.id))
    logger.warn(
      `[ScheduledTask #${task.id}] 已重试 ${newRetryCount}/${maxRetries} 次，自动禁用。` +
      `请检查任务配置或目标对象状态。`
    )
  } else {
    // 指数退避：5分 × 2^(retryCount-1)
    const backoffMinutes = 5 * Math.pow(2, newRetryCount - 1)
    const nextRetry = now.plus({ minutes: backoffMinutes }).toJSDate()
    await db.update(scheduledTasks)
      .set({
        lastStatus: 'fail',
        retryCount: newRetryCount,
        nextExecuteAt: nextRetry,
        lastExecutedAt: now.toJSDate()
      })
      .where(eq(scheduledTasks.id, task.id))
    logger.info(
      `[ScheduledTask #${task.id}] 第 ${newRetryCount}/${maxRetries} 次失败，` +
      `${backoffMinutes} 分钟后重试`
    )
  }
}

/** 跳过时更新：重新计算 next_execute_at 前推到下一周期，避免死循环（R2#3） */
async function updateTaskAfterSkip(task) {
  const nextAt = await computeNextExecuteAt(task)  // M1: async 函数需 await
  await db.update(scheduledTasks)
    .set({
      lastStatus: 'skipped',
      nextExecuteAt: nextAt  // 前推到下一周期，防止每分钟重复 skip
    })
    .where(eq(scheduledTasks.id, task.id))
}

/**
 * 从任务配置计算下次执行时间（复用于 updateTaskAfterExecute 和 updateTaskAfterSkip）
 * M1 修复：ESM 模块使用 await import() 而非 require()
 */
async function computeNextExecuteAt(task) {
  const zone = task.scheduleTimezone || 'UTC'
  const now = DateTime.now().setZone(zone)

  switch (task.scheduleType) {
    case 'once':
      return null  // 一次性任务无需 next
    case 'daily': {
      const [hh, mm] = (task.scheduleAt || '00:00').split(':').map(Number)
      return now.plus({ days: 1 }).set({ hour: hh, minute: mm, second: 0, millisecond: 0 }).toUTC().toJSDate()
    }
    case 'weekly': {
      const [weekDays, timeStr] = (task.scheduleAt || '').split('|')
      const allowed = weekDays.split(',').map(Number)
      const [hh, mm] = timeStr.split(':').map(Number)
      for (let d = 1; d <= 7; d++) {
        const candidate = now.plus({ days: d })
        if (allowed.includes(candidate.weekday)) {
          return candidate.set({ hour: hh, minute: mm, second: 0, millisecond: 0 }).toUTC().toJSDate()
        }
      }
      return null
    }
    case 'cron': {
      try {
        // M1 修复：ESM 模块必须使用 await import，不可用 require
        const { default: cronParser } = await import('cron-parser')
        const interval = cronParser.parseExpression(task.scheduleCron || '* * * * *', { tz: zone })
        return interval.next().toDate()
      } catch {
        return null
      }
    }
    default:
      return null
  }
}

/**
 * 写入审计日志
 * S1 修复：非 ad 级任务从 structure_ads 获取关联 ad_id，填充 auditAdId
 */
async function writeAuditLog(task, status, preflightMode, errorMessage) {
  // S1：非 ad 级操作从 structure_ads 获取一个关联广告 ID，用于审计日志追溯
  let auditAdId = ''
  if (task.targetLevel === 'ad') {
    auditAdId = String(task.targetId)
  } else {
    try {
      const filterCol = task.targetLevel === 'adset' ? 'adset_id' : 'campaign_id'
      const [rows] = await pool.execute(
        `SELECT ad_id FROM structure_ads WHERE account_id = ? AND ${filterCol} = ? LIMIT 1`,
        [task.accountId, task.targetId]
      )
      auditAdId = String(rows?.[0]?.ad_id || task.targetId)
    } catch {
      auditAdId = String(task.targetId)  // 优雅降级：用 targetId 兜底
    }
  }

  try {
    await db.insert(automationLogs).values({
      accountId: String(task.accountId),
      adId: auditAdId,
      objectType: task.targetLevel,
      objectId: String(task.targetId),
      actionType: String(task.actionType).toUpperCase(),
      actionPayload: typeof task.actionParams === 'string'
        ? JSON.parse(task.actionParams)
        : (task.actionParams || {}),
      explanation: JSON.stringify({
        trigger_type: 'scheduled',
        schedule_type: task.scheduleType,
        schedule_at: task.scheduleAt,
        task_id: task.id
      }),
      preflightMode: preflightMode || (task.isSimulation ? 'preflight' : null),
      status,
      errorMessage,
      isSimulation: task.isSimulation || false,
      triggeredAt: new Date(),
      ownerId: task.ownerId || 0
    })
  } catch (e) {
    logger.error(`[ScheduledTask #${task.id}] 审计日志写入失败:`, e.message)
  }
}
```

### 4.3 Cron 注册（在 `cronService.js` 的 `startCronJob` 中追加）

```js
// 在 startCronJob() 末尾、现有 7 条 cron.schedule 之后追加第 8 条：

// 8. 定时任务调度：每分钟检查到期的 timed tasks
// 二次审查 R2#4：不在此处定义锁，而是调用 service 导出的检查函数
import { executeDueScheduledTasks, isScheduledTaskRunning } from './scheduledTaskService.js'

cron.schedule('* * * * *', async () => {
  if (isScheduledTaskRunning()) {
    logger.warn('[ScheduledTask Cron] 上一轮尚未执行完毕，跳过本轮（防并发重叠）')
    return
  }
  try {
    const result = await executeDueScheduledTasks()
    if (result.executed > 0 || result.errors > 0) {
      logger.info(
        `[ScheduledTask Cron] 本次处理 ${result.executed + result.skipped + result.errors} 条，` +
        `执行 ${result.executed}，跳过 ${result.skipped}，失败 ${result.errors}`
      )
    }
  } catch (err) {
    logger.error('[ScheduledTask Cron] 异常:', err.message)
  }
})
```

---

## 五、复用现有接口清单

| 功能 | 复用的接口/服务 | 来源文件 |
|---|---|---|
| 读取广告账户列表 | `accountSyncService.getAccountsFromDatabase()` | `server/services/accountSyncService.js` |
| 读取广告结构（ad/campaign/adset） | `structureSyncService.listStructureObjectsFromDb()` | `server/services/structureSyncService.js` |
| 暂停广告 | `FacebookMarketingAPI.pauseAd(adId)` | `server/index.js` |
| 激活广告 | `FacebookMarketingAPI.activateAd(adId)` | `server/index.js` |
| 暂停广告组 | `FacebookMarketingAPI.pauseAdset(adsetId)` | `server/index.js` |
| 激活广告组 | `FacebookMarketingAPI.activateAdset(adsetId)` | `server/index.js` |
| 暂停广告系列 | `FacebookMarketingAPI.pauseCampaign(campaignId)` | `server/index.js` |
| 激活广告系列 | `FacebookMarketingAPI.activateCampaign(campaignId)` | `server/index.js` |
| 查询广告组预算 | `FacebookMarketingAPI.getAdsetBudget(adsetId)` | `server/index.js` |
| 更新广告组预算 | `FacebookMarketingAPI.updateAdsetBudget(adsetId, cents)` | `server/index.js` |
| 更新广告系列预算 | `FacebookMarketingAPI.updateCampaignBudget(cid, cents)` | `server/index.js` |
| 计算目标预算（set/increase/decrease） | `actionExecutorService.computeNewBudgetCentsOnce()` | `server/services/actionExecutorService.js` |
| 审计日志写入 | `automationLogs` 表（Drizzle insert） | `server/db/schema.js` |
| Facebook API 限流/熔断 | `rateLimitService`（通过 index.js 的 makeRequest 自动生效） | `server/services/rateLimitService.js` |
| 代理/SOCKS5 | `index.js` 中的代理探测 + socks5 模块（通过 makeRequest 自动生效） | `server/index.js` + `server/socks5.js` |

---

## 六、改动文件清单（v2.0 更新）

| 序号 | 文件 | 操作 | 说明 |
|---|---|---|---|
| 1 | `server/db/schema.js` | **编辑** | 新增 `scheduledTasks` 表定义 |
| 2 | `server/db/migrations/052_create_scheduled_tasks.sql` | **新建** | 建表迁移脚本 |
| 2+ | `server/db/migrations/053_add_dynamic_scope_to_scheduled_tasks.sql` | **v2.0 新建** | 多对象支持：6 个 ALTER |
| 2++ | `server/db/migrations/055_add_multi_account_to_scheduled_tasks.sql` | **v3.0 新建** | 多账户支持：2 个 ALTER |
| 3 | `server/services/scheduledTaskService.js` | **新建** | 核心服务：时间判断 + 多账户 + 多对象调度执行 |
| 4 | `server/routes/scheduledTasks.js` | **新建** | CRUD API 路由 |
| 5 | `server/services/cronService.js` | **编辑** | `startCronJob()` 中追加 1 条 `cron.schedule` |
| 6 | `server/app.js` | **编辑** | 注册 `/api/scheduled-tasks` 路由 |
| 7 | `src/views/ScheduledTasks.vue` | **新建** | 定时任务管理页面（v2.0 checkbox 多选） |
| 8 | `src/router/index.js` | **编辑** | 新增 `/scheduled-tasks` 路由 |
| 9 | `src/services/facebookApi.js` | **编辑** | 新增 `scheduledTasksApi` 方法 |
| 10 | `src/layouts/MainLayout.vue` | **编辑** | 导航栏入口（目前注释隐藏） |
| — | `package.json` | **编辑** | 新增 `cron-parser` 依赖 |

**现有文件改动汇总**：仅 5 个文件有少量追加，**不修改任何现有逻辑**。

### 预算单位约定

全链路使用**美分（cents）** 作为内部单位，仅在入口和出口做转换：

```
用户输入 ($100.00)
  │ 前端：value = 100（美元）
  ▼
action_params.value = 100  ← 存储为美元
  │
  ▼
executeAction 内直接计算（v1.6）:
  set_budget      → newCents = Math.round(100 * 100) = 10000 美分
  increase/decrease → 走 computeNewBudgetCentsOnce()（该函数对 set_budget 仅上调，但定时任务不走它）
  │
  ▼
api.updateAdsetBudget(id, newCents)
  │ FB API 接收美分
  ▼
实际预算 = $100.00

max_daily_budget / min_daily_budget ← 存储和传递均为美分（不做 ×100）
```

---

## 七、API 设计

### 7.1 路由

```
GET    /api/scheduled-tasks              # 列表查询（支持 ?account_id=&status=enabled）
POST   /api/scheduled-tasks              # 创建定时任务
GET    /api/scheduled-tasks/:id          # 查看单条
PUT    /api/scheduled-tasks/:id          # 编辑
DELETE /api/scheduled-tasks/:id          # 删除
POST   /api/scheduled-tasks/:id/execute  # 手动立即执行（force）
PATCH  /api/scheduled-tasks/:id/toggle   # 启用/禁用
```

所有端点在 `routes/scheduledTasks.js` 中需挂载鉴权中间件（四轮审查#10）：

```js
// server/routes/scheduledTasks.js
import { Router } from 'express'
import { requireAuth, requireActive } from '../middleware/authJwt.js'

const router = Router()
router.use(requireAuth)
router.use(requireActive)
```

### 7.2 创建/更新的自动计算

服务端在创建时自动计算 `next_execute_at`（UTC）。更新（PUT）时，若 `schedule_type` / `schedule_at` / `schedule_cron` / `schedule_timezone` 任一变更，自动触发 `calcNextExecuteAt()` 重算（四轮审查#8）。

### 7.3 创建请求示例（v2.0 多对象模式）

```json
POST /api/scheduled-tasks
{
  "schedule_type": "once",
  "schedule_at": "2026-06-03 14:00",
  "schedule_timezone": "Asia/Shanghai",
  "account_id": "act_123456789",
  "target_level": "ad",
  "target_ids": ["123456789012345", "123456789012346"],
  "action_type": "set_budget",
  "action_params": { "value": 100 },
  "use_dynamic_scope": false,
  "scope_filters": null,
  "exclude_ids": null,
  "max_dynamic_matches": 1000,
  "is_simulation": false,
  "auto_disable": true
}
```

```json
// 动态筛选模式
{
  "schedule_type": "daily",
  "schedule_at": "08:00",
  "account_id": "act_123456789",
  "target_level": "ad",
  "target_ids": [],
  "use_dynamic_scope": true,
  "scope_filters": {
    "level": "ad",
    "conditions": [
      { "field": "name", "operator": "contains", "value": "核心" },
      { "field": "effective_status", "operator": "in", "value": ["ACTIVE"] }
    ]
  },
  "exclude_ids": { "ad_ids": ["999999999999999"], "adset_ids": [], "campaign_ids": [] },
  "max_dynamic_matches": 500,
  "action_type": "pause_ad",
  "action_params": {}
}
```

### 7.4 `calcNextExecuteAt` 函数实现

服务端在创建/更新时自动计算 `next_execute_at`（UTC）：

```js
/**
 * 二次审查 R2#5：若 schedule_timezone 为 NULL，回退到账户时区（查询 account_mappings）
 */
async function calcNextExecuteAt(scheduleType, scheduleAt, scheduleCron, scheduleTimezone, accountId) {
  // R2#5：NULL 时回退到账户时区，而非硬编码 UTC
  let zone = scheduleTimezone
  if (!zone) {
    try {
      const [rows] = await pool.execute(
        'SELECT timezone_name FROM account_mappings WHERE fb_account_id = ? LIMIT 1',
        [accountId]
      )
      zone = rows?.[0]?.timezone_name || 'UTC'
    } catch {
      zone = 'UTC'
    }
  }
  const now = DateTime.now().setZone(zone)

  switch (scheduleType) {
    case 'once': {
      const [datePart, timePart] = scheduleAt.split(' ')
      return DateTime.fromISO(`${datePart}T${timePart}:00`, { zone }).toUTC().toJSDate()
    }
    case 'daily': {
      const [hh, mm] = scheduleAt.split(':').map(Number)
      let target = now.set({ hour: hh, minute: mm, second: 0, millisecond: 0 })
      if (target <= now) target = target.plus({ days: 1 })
      return target.toUTC().toJSDate()
    }
    case 'weekly': {
      const [weekDays, timeStr] = scheduleAt.split('|')
      const allowed = weekDays.split(',').map(Number)
      const [hh, mm] = timeStr.split(':').map(Number)
      for (let d = 0; d <= 7; d++) {
        const candidate = now.plus({ days: d }).set({ hour: hh, minute: mm, second: 0, millisecond: 0 })
        if (allowed.includes(candidate.weekday) && candidate > now) {
          return candidate.toUTC().toJSDate()
        }
      }
      return null
    }
    case 'cron': {
      // 审查问题7：cron 类型使用 cron-parser 计算 next
      try {
        const { default: cronParser } = await import('cron-parser')
        const interval = cronParser.parseExpression(scheduleCron || '* * * * *', { tz: zone })
        return interval.next().toDate()
      } catch (e) {
        logger.error(`[ScheduledTask] cron 表达式解析失败: ${scheduleCron}`, e.message)
        return null
      }
    }
    default:
      return null
  }
}
```

---

## 八、前端交互设计

### 8.1 定时任务列表页 `ScheduledTasks.vue`

```
┌──────────────────────────────────────────────────────┐
│  📋 定时任务               [+ 新建定时任务]          │
├──────────────────────────────────────────────────────┤
│  筛选：[全部账户 ▼] [全部状态 ▼]                     │
├──────┬──────────┬──────────┬──────────┬──────────────┤
│ 账户 │ 目标广告  │ 动作     │ 下次执行   │ 状态  操作  │
├──────┼──────────┼──────────┼──────────┼──────────────┤
│ act_ │ 广告A    │ 调预算   │ 06-03    │ ⏳等待  ⋮  │
│ 123  │ (ID:456) │ →$100   │ 14:00    │          │
│      │          │          │          │          │
│ act_ │ 广告B    │ 暂停     │ 每天     │ ✅启用  ⋮  │
│ 456  │ (ID:789) │          │ 08:00    │          │
│      │          │          │          │          │
│ act_ │ 广告组X  │ 启动     │ 每周一三五 │ ✅启用  ⋮  │
│ 789  │ (ID:012) │          │ 09:00    │          │
└──────┴──────────┴──────────┴──────────┴──────────────┘
```

### 8.2 创建/编辑弹窗（v2.0 多对象版）

```
┌─────────────────────────────────────────────┐
│  新建定时任务                          [✕]  │
├─────────────────────────────────────────────┤
│  1. 基本信息：                              │
│    调度类型：[一次性 ▼]  时区：[UTC ▼]      │
│    执行时间：📅 [2026-06-03] ⏰ [14:00]     │
│                                              │
│  2. 作用对象 (Where)：                      │
│    广告账户：[act_123 ▼] [全选] [清空]      │
│    目标层级：● 广告  ○ 广告组  ○ 广告系列   │
│                                              │
│    ┌─ 动态筛选 ──────────────────────────┐  │
│    │ 开关 [OFF]  匹配上限 [1000     ]     │  │
│    │ 监控范围条件：                       │  │
│    │   名称 [包含 ▼] [________]  [×]     │  │
│    │   状态 [等于 ▼] [投放中 ▼]   [×]    │  │
│    │   [+ 添加监控范围 (2/3)]             │  │
│    └──────────────────────────────────────┘  │
│                                              │
│    选择目标对象（可多选）：                 │
│    ┌──────────────────────────────────────┐  │
│    │ 🔍 [搜索名称或ID________]            │  │
│    │ 已选 5 个  [全选] [清空]            │  │
│    │ [✕ 广告A] [✕ 广告B] ... 还有2个     │  │
│    │ ☑ 广告A  ACTIVE                     │  │
│    │ ☑ 广告B  ACTIVE                     │  │
│    │ ☐ 广告C  PAUSED                     │  │
│    └──────────────────────────────────────┘  │
│                                              │
│    排除名单（可选）：                       │
│    ┌──────────────────────────────────────┐  │
│    │ 🔍 [搜索名称或ID________]            │  │
│    │ 已排除 1 个  [全选] [清空]          │  │
│    │ ☑ 广告Z  [从排除]                   │  │
│    └──────────────────────────────────────┘  │
│                                              │
│  3. 执行动作：设置预算到 → [$100.00]        │
│  4. 选项：☐ Dry Run  ☑ 执行后自动禁用     │
│                                              │
│  📅 预计下次: 2026-06-03 14:00 CST          │
│              [取消]  [创建]                   │
└─────────────────────────────────────────────┘
```

---

## 九、校验规则

### 9.1 创建时（v2.0 更新）

| 校验项 | 规则 |
|---|---|
| `schedule_type` | 必填，`once/daily/weekly/cron` |
| `schedule_at` | 非 cron 必填；once 必须 > 当前时间+2分钟 |
| `account_id` | 必填，存在且 `is_active=1` |
| `target_ids` | 手动模式：至少 1 个；动态模式：可为空，改由 `scope_filters` 确定 |
| `use_dynamic_scope` | 若为 true，`scope_filters` 必填且至少 1 条有效条件 |
| `action_type` + `action_params` | 必须匹配；`set_budget` 的 value > 0 |
| `exclude_ids` | 可选，格式 `{ ad_ids: [...], adset_ids: [...], campaign_ids: [...] }` |
| `max_dynamic_matches` | 可选，默认 1000 |
| 用户权限 | `user_id` 从 JWT 提取，须有 `account_id` 访问权限 |

### 9.2 启用时

```
enabled=true 时额外校验：
  ├─ once 类型且 schedule_at 已过期 → 拒绝启用
  └─ target_id 仍存在于 structure 表中 → 若不存在则 WARN 但允许启用
          （广告可能被删除后重建，保留任务等用户手动处理）
```

---

## 十、审计日志格式

写入 `automation_logs` 时，`explanation` JSON 增加 `trigger_type: "scheduled"`：

```json
{
  "trigger_type": "scheduled",
  "schedule_type": "once",
  "schedule_at": "2026-06-03 14:00",
  "task_id": 42,
  "action": "set_budget",
  "target_id": "123456789012345"
}
```

现有 `automation_logs` 表结构完全兼容。`ruleId` 字段可为 NULL（定时任务无规则ID），前端日志页根据 `explanation.trigger_type` 区分展示「条件触发」「定时触发」标签。

### 批量写入优化（R2#6）

当 100 条任务同时到期时，逐条 insert 性能较低。建议收集为数组后批量写入：

```js
// executeDueScheduledTasks 内
const auditBatch = []

// 每条任务执行后 push 到数组，而非立即 insert
for (const task of dueTasks) {
  // ... execute ...
  auditBatch.push(buildAuditLogRecord(task, 'success', null, null))
}

// 循环结束后一次批量写入
if (auditBatch.length > 0) {
  try {
    await db.insert(automationLogs).values(auditBatch)
  } catch (e) {
    logger.error(`[ScheduledTask] 批量审计日志写入失败 (${auditBatch.length} 条):`, e.message)
  }
}
```

### 手动执行路径（v2.0）

`POST /api/scheduled-tasks/:id/execute` 的 force 执行路径：

```
force 执行（v2.0 多对象）：
  ├─ 不经过互斥锁检查
  ├─ resolveTargetIdsForTask() → 解析所有目标
  ├─ 遍历每个 targetId 执行 executeAction
  ├─ 逐个写审计日志
  ├─ 部分失败不影响其他对象
  └─ 返回 "执行完成: N 成功, M 失败"
```

---

## 十一、实施步骤（v2.0 状态）

| 阶段 | 任务 | 状态 |
|---|---|---|
| **Phase 1** | 建表迁移 SQL + Drizzle Schema | ✅ 完成 |
| **Phase 2** | `scheduledTaskService.js` 核心服务 | ✅ 完成 |
| **Phase 3** | `routes/scheduledTasks.js` CRUD API | ✅ 完成 |
| **Phase 4** | `cronService.js` + `app.js` 注册 | ✅ 完成 |
| **Phase 5** | 后端联调 + API 测试 | ✅ 完成 |
| **Phase 6** | 前端 `ScheduledTasks.vue`（含多选+排除名单） | ✅ 完成 |
| **Phase 6+** | 前端 `Logs.vue` 增加 `trigger_type` 标签 | ❌ 待完成 |
| **Phase 7** | 前端路由 + API 方法 | ✅ 完成 |
| **Phase 8** | 端到端验证（创建→等待执行→查看审计日志） | ✅ 完成（Dry Run 验证通过） |
| **v2.0** | 多对象支持：DB 迁移 + 遍历执行 + 前端 checkbox | ✅ 完成 |
| **v2.1** | 任务名称 + 预算上下限 + 审计日志增强 + UI 整合 | ✅ 完成 |

### 新增依赖

```bash
npm install cron-parser@^4.0.0   # cron 类型任务的 next_execute_at 计算（四轮审查#9：5.x 不存在，修正为 4.x）
```

### 与条件规则的交互约定（四轮审查#3）

定时任务与条件规则**独立运作**，不互斥、不互锁：

| 场景 | 行为 |
|---|---|
| 定时任务 pause + 条件规则 activate | 可能产生"乒乓效应"——两个系统独立判断，轮流操作同一对象 |
| 定时任务刚执行完 set_budget | 条件规则下一分钟评估时用的仍是旧指标值（定时任务不更新 `ad_snapshots`） |
| 冲突规避 | **用户自行协调**，不在同一对象上设置冲突的定时任务和条件规则 |

所有定时任务的审计日志均标记 `explanation.trigger_type = 'scheduled'`，前端日志页可据此区分操作来源。

---

## 十二、迁移 SQL

### 052: 建表

```sql
-- 052_create_scheduled_tasks.sql
CREATE TABLE IF NOT EXISTS scheduled_tasks (
  id              INT AUTO_INCREMENT PRIMARY KEY,
  user_id         INT NOT NULL,
  owner_id        INT NOT NULL DEFAULT 0,
  schedule_type   VARCHAR(16) NOT NULL,
  schedule_at     VARCHAR(32) NULL,
  schedule_cron   VARCHAR(64) NULL,
  schedule_timezone VARCHAR(50) NULL,
  next_execute_at DATETIME NULL,
  account_id      VARCHAR(50) NOT NULL,
  target_level    VARCHAR(16) NOT NULL DEFAULT 'ad',
  target_id       VARCHAR(50) NULL,             -- v2.0 改为可空
  target_ids      JSON NULL,                    -- v2.0 新增
  use_dynamic_scope TINYINT(1) NOT NULL DEFAULT 0,  -- v2.0 新增
  scope_filters   JSON NULL,                    -- v2.0 新增
  exclude_ids     JSON NULL,                    -- v2.0 新增
  max_dynamic_matches INT NOT NULL DEFAULT 1000, -- v2.0 新增
  action_type     VARCHAR(32) NOT NULL,
  action_params   JSON NOT NULL,
  enabled         TINYINT(1) NOT NULL DEFAULT 1,
  is_simulation   TINYINT(1) NOT NULL DEFAULT 0,
  auto_disable    TINYINT(1) NOT NULL DEFAULT 1,
  last_executed_at DATETIME NULL,
  last_status     VARCHAR(16) NULL,
  retry_count     INT NOT NULL DEFAULT 0,
  max_retries     INT NOT NULL DEFAULT 3,
  version         INT NOT NULL DEFAULT 0,
  created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX idx_next_execute (enabled, next_execute_at),
  INDEX idx_account (account_id),
  INDEX idx_user (user_id),
  INDEX idx_owner (owner_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 053: v2.0 多对象支持

```sql
ALTER TABLE scheduled_tasks MODIFY COLUMN target_id VARCHAR(50) NULL;
ALTER TABLE scheduled_tasks ADD COLUMN target_ids JSON NULL AFTER target_id;
ALTER TABLE scheduled_tasks ADD COLUMN use_dynamic_scope TINYINT(1) NOT NULL DEFAULT 0 AFTER target_ids;
ALTER TABLE scheduled_tasks ADD COLUMN scope_filters JSON NULL AFTER use_dynamic_scope;
ALTER TABLE scheduled_tasks ADD COLUMN exclude_ids JSON NULL AFTER scope_filters;
ALTER TABLE scheduled_tasks ADD COLUMN max_dynamic_matches INT NOT NULL DEFAULT 1000 AFTER exclude_ids;
```

### 054: v2.1 任务名称

```sql
ALTER TABLE scheduled_tasks ADD COLUMN task_name VARCHAR(255) DEFAULT NULL COMMENT '任务名称（用户自定义）' AFTER action_params;
```

### 055: v3.0 多账户支持

```sql
ALTER TABLE scheduled_tasks ADD COLUMN target_account_ids JSON NULL AFTER account_id;
ALTER TABLE scheduled_tasks ADD COLUMN target_by_account JSON NULL AFTER target_account_ids;
```

### 清理策略（R2#7）

`once` 类型任务执行后 `enabled=false`，会累积在表中。建议定期清理：

```sql
-- 清理 90 天前已禁用的 once 类型定时任务
DELETE FROM scheduled_tasks
WHERE enabled = 0
  AND schedule_type = 'once'
  AND updated_at < NOW() - INTERVAL 90 DAY;
```

可加入现有的历史表清理 Cron（`cron.schedule('30 4 * * *', ...)`）中一并执行。具体做法：在 `server/services/cronService.js` 的 `runNightlyHistoryCleanup()` 函数中追加此 DELETE 语句（五轮审查#3.2）。

---

## 十三、风险与边界

| 风险 | 应对 |
|---|---|
| 服务器宕机错过 once 任务 | `next_execute_at <= NOW()` + 2 分钟容差窗口兜底——重启后首次 Cron 扫描会补偿执行 |
| once 任务已过期（超过 `schedule_at` 2分钟以上） | 查询条件 `next_execute_at >= NOW() - INTERVAL 2 MINUTE` 排除，不会执行 |
| daily 任务在 DST（夏令时）切换日 | 使用 `schedule_timezone` 时区计算，Luxon 自动处理 DST |
| 同一任务被并发执行 | Cron 回调入口有互斥锁（`scheduledTaskRunning` 标志），防止重叠；SQL UPDATE 是原子的 |
| 任务持续失败打爆 API 配额 | 指数退避重试（5分→10分→20分），超 `maxRetries` 自动禁用，不会无限重试 |
| 广告已被删除 | 执行前查 `structure_ads` 确认存在，不存在则跳过 + 重试退避 → 最终禁用 |
| 调预算时广告组不存在 | 同理，跳过 + 重试退避 → 最终禁用 |
| campaign 级预算操作调错 API | 已按 `targetLevel` 分流：campaign→`getCampaignBudget`/`updateCampaignBudget`，ad/adset→`getAdsetBudget`/`updateAdsetBudget` |

---

## 十四、与方案一的最终对比

| 评判标准 | 方案一 | 方案二 |
|---|---|---|
| 改动现有文件 | 6 个 | 3 个（仅追加，不改逻辑） |
| 新增文件 | 1 个迁移 | 1 迁移 + 2 服务 + 1 路由 + 1 前端 |
| 改动核心函数 `executeRulesForAccount` | ✅ 是 | ❌ 否 |
| 改动 `conditionsValidator` | ✅ 是 | ❌ 否 |
| 改动 `rules` 表结构 | ✅ 是（6 个新字段） | ❌ 否 |
| 与 condition 规则共享账户锁 | ✅ 是 | ❌ 否（独立 Cron） |
| SQL 查询效率 | 全量拉取 rules 再 JS 过滤 | 精确索引 `idx_next_execute` |
| 回滚难度 | 中（回滚表结构+6文件） | 低（删表+删文件） |
| 前端改造 | 改造 RuleManager.vue | 新建 ScheduledTasks.vue |

---

## 十五、负责人（Owner）筛选逻辑（v3.1）

> 日期：2026-06-08
> 状态：✅ 已实施

复用规则管理的负责人筛选模式，定时任务列表支持按负责人维度过滤。

### 15.1 后端（`server/routes/scheduledTasks.js`）

**GET /api/scheduled-tasks 新增查询参数：**

| 参数 | 类型 | 说明 |
|------|------|------|
| `ownerIds` | string | 逗号分隔的负责人 ID，如 `"1,2,3"`。仅管理员可用，不传=全部 |
| `includeNoOwner` | string | `"1"` 表示同时筛选管理员创建的规则（负责人显示为"无"）。仅管理员可用 |

**权限分级：**

| 角色 | 行为 |
|------|------|
| 普通用户 | 只看自己同 `owner_id` 维度的任务（非仅自己的任务，而是同负责人的所有用户的定时任务） |
| 管理员 | 默认看全部；可选按 `ownerIds` / `includeNoOwner` 筛选 |

**响应新增字段：**
- `isAdmin` — 当前用户是否为管理员，前端据此显示负责人筛选 UI
- 每条任务增加 `ownerId`、`ownerName`（从 `users` → `owners` 联表查询）

**实现方式：**
- 通过 `pool.execute` 查询 `users` 表的 `owner_id` 映射关系
- 将 `ownerIds` 转换为对应的 `user_id` 列表，用 Drizzle `or(...)` 条件过滤 `scheduled_tasks.userId`
- `enrichTaskOwnerNames()` 函数批量补全每条任务的负责人名称

### 15.2 前端（`src/views/ScheduledTasks.vue`）

**新增状态：**
- `isAdminFromTasks` — 是否管理员（控制筛选 UI 显示）
- `ownersList` — 负责人列表（从 `/api/owners` 加载）
- `selectedOwnerIds` — 选中的负责人 ID 列表
- `includeNoOwner` — 是否包含"管理员创建"
- `ownerFilterOpen` — 下拉展开状态
- `ownerFilterDisplayValue` — 计算属性，显示当前筛选文字

**UI 变更：**
- 筛选栏增加负责人下拉多选组件（与 RuleManager 同款 UI）
- 任务卡片增加负责人信息行（仅管理员可见）
- `loadTasks()` 传递 `ownerIds` / `includeNoOwner` 查询参数
- 切换筛选后自动重新加载列表
